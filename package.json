{
  "name": "agentide",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Agent Runtime Platform \u2014 monorepo root",
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "build": "tsc --build --pretty",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "gateway": "agentide start --all-doors --foreground",
    "gateway:log": "agentide start --all-doors --foreground --log-file /tmp/gateway.log",
    "precommit": "bash scripts/check-banned-types.sh && npm run typecheck && npm run lint && npm run build",
    "lint": "eslint .",
    "typecheck": "test -z \"$(ls -A packages 2>/dev/null)\" && echo 'no packages yet \u2014 skipping typecheck' || tsc --build --pretty"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "eslint": "^10.8.0",
    "@typescript-eslint/parser": "^8.65.0",
    "@typescript-eslint/eslint-plugin": "^8.65.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
