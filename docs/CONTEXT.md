# CONTEXT.md

Glossary + standing conventions for the Agentide project, maintained as features are built.
This is a working reference for `feature-pipeline` grilling sessions — a stability signal is
a term or decision already captured here rather than re-litigated per feature.

Full architecture docs live in `docs/architecture/` (Vision, Goals, Agentide, Terminology,
Core Concept, Capability System, Business/Platform/Runtime Capabilities, Architectural
Refinement V1, Plugin Marketplace). This file is the condensed, code-facing version — update
it whenever a `feature-pipeline` run settles something new. `docs/Feature_Backlog.md` and
`docs/drift-issue-log.md` track sequencing and known design gaps respectively.

**Governing philosophy:** [PHILOSOPHY.md](/home/spanexx/Shared/Learn/Agent-Bridge-SDK/PHILOSOPHY.md)
governs every architecture decision. Every component, interface, and dependency must satisfy
the replaceability test: *"If we replace this component tomorrow, how much of the rest must
change?"* The ideal answer is nothing.

***

## Glossary

| Term                      | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Platform**              | The whole system: Gateway, Session Manager, Capability Registry, Plugin Manager, Event Bus, SDKs, Runtimes, Adapters, Dashboard                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Application**           | Software built by a developer that connects to the platform via an SDK — never installed into the platform itself                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **SDK**                   | A role (Backend or Frontend), not a single package — see SDK Naming Convention below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Capability**            | Smallest invocable unit — `<domain>.<action>`, typed `business` / `platform` / `runtime`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Capability Registry**   | Catalog of every capability; discovery only, no execution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Gateway**               | Entry point — auth, session creation, discovery, routing. Never executes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Control Plane**         | Gateway + Session Manager + Capability Registry + Plugin Manager — coordinates, doesn't execute                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Execution Plane**       | All Runtimes (Browser, Backend, Docker, Git, etc.) — executes, doesn't coordinate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Session**               | An execution context (not chat history) — owns its runtime resources, auto-cleaned on destroy. Lifecycle: Active (running) ⇄ Suspended (paused, resources retained) → Archived (soft-delete, metadata retained for TTL)                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Session Manager**       | Creates/resumes/destroy sessions. Tracks runtime resources per session. Part of the Control Plane                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Session Suspend**       | Moving a session from Active to Suspended — resources preserved, execution paused. Triggered by idle timeout or Gateway policy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Session Resume**        | Moving a session from Suspended to Active — resources restored, execution continues. Called by Gateway via session ID only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Session Archive**       | Soft-delete state after destroy — session metadata kept for configurable TTL, resources already cleaned up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Runtime**               | An execution environment (Browser Runtime, Backend Runtime, etc.), owns its own resources                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Adapter**               | Pure protocol translator (MCP, CLI, REST, WebSocket) — no business logic, no state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Audit Log**             | Append-only structured log (default `./data/audit.log`) where the Gateway records one JSON object per capability invocation. Mirrored on the Event Bus as `gateway.invocation` for downstream consumers. Records `caller`, `session`, `capability`, `owner`, `status` (`ok` / `denied` / `error`), `denyReason` / `errorCode`, `durationMs`. Input payloads are NOT logged (PII stays in the application)                                                                                                                                                                                                                                                                       |
| **Tenant**                | An isolated organization within a platform installation. v1 gateway-core ships full tenant-lifecycle capabilities (`tenant.create`, `tenant.list`, `tenant.suspend`, `tenant.delete`) usable by both self-hosted operators (who may run a single tenant per install) and hosted platforms (who provision many tenants per process). Every token's `sub` is `{ tenantId, callerId }`; every audit record, rate-limit bucket, and session record carries `tenantId`. The Gateway refuses any cross-tenant operation. Self-hosted operators ignore `tenant.create` and live with one auto-provisioned tenant; hosted platforms use the full lifecycle                              |
| **Capability Invocation** | The canonical unit of work the Gateway routes: `{ caller, session?, capability, input } → { output } or { error }`. Every platform, business, and runtime capability call is shaped this way inside the Gateway, regardless of which adapter (MCP/CLI/REST/WS) the caller used                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Plugin**                | Extends the platform without touching core — Runtime Plugin / Service Plugin / Developer Plugin                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Plugin Manager**        | Installs/updates/removes plugins from a Plugin Manifest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Plugin Manifest**       | Declarative plugin identity + version + capabilities it registers. Top-level key indicates the plugin type: `runtime:`, `service:`, or `developer:` (exactly one per manifest) — the key both names the type and contains the plugin's `id`. Runtime example: `runtime: { id: browser }`. Service plugins expose no capabilities (observe-only); developer plugins likewise.                                                                                                                                                                                                                                                                                                    |
| **Capability Manifest**   | An application's declarative capability list, published at startup                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Event / Event Bus**     | Immutable fact + the pub/sub delivery mechanism between components. Custom (not EventEmitter). Sync dispatch in subscription order. Async handlers via `Promise.allSettled`. One handler failure never blocks others. Dot-delimited prefix wildcard: `*` as final segment matches any remaining depth (e.g. `browser.*` matches `browser.page.loaded`); bare `*` matches every event. `Object.freeze()` shallow immutability at publish. Subscribe returns `Subscription` with `.unsubscribe()`. Failures surfaced as `event.handler_failed` with `{ eventName, subscriberPattern, error: { message, stack? } }` — never silent. `event.*` is reserved for bus-internal events. |
| **Resource**              | Anything owned by a session (browser tab, temp file, DB transaction) — cleaned up with the session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Tenant**                | Isolated org/customer in a hosted deployment — **not yet fully designed**, see Open Items                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

***

## Established Conventions (apply to every feature, not just one)

### Capability typing

Every capability is `business` (app-owned), `platform` (platform-core-owned), or `runtime`
(runtime-plugin-owned). Classification test: what does it touch — your app's data, the
platform's own internals, or an external execution environment?

### Permission tiering

Runtime Capabilities use a three-tier scope convention:

```
runtime.<namespace>.read          — observe only
runtime.<namespace>.act           — normal, reversible action
runtime.<namespace>.destructive   — irreversible / high-impact action
```

Platform Capabilities use a two-tier read/write split (e.g. `platform.plugin.read` vs.
`platform.plugin.install`). Business Capabilities don't need tiering today — each one is
already a single named action.

### SDK naming

"Backend SDK" and "Frontend SDK" are roles, not packages. Concrete packages:

| Role     | Language        | Package                                                  |
| -------- | --------------- | -------------------------------------------------------- |
| Backend  | Node/TypeScript | `@platform/sdk-node` (reference implementation, Phase 3) |
| Backend  | Python          | `platform-sdk-python`                                    |
| Backend  | Go              | `platform-sdk-go`                                        |
| Backend  | Rust            | `platform-sdk-rust`                                      |
| Backend  | Java            | `platform-sdk-java`                                      |
| Backend  | .NET            | `platform-sdk-dotnet` (NuGet: `Platform.Sdk`)            |
| Frontend | Browser only    | `@platform/sdk-browser` — no per-language variants       |

### Deployment model permissions

Self-hosted: the operator may hold the full `platform.*` range. Hosted/SaaS: write-tier
`platform.plugin.*` (and similarly infrastructure-affecting) permissions are reserved for the
platform operator, never an individual tenant. Tenants get Business Capability registration
(not a `platform.*` permission at all) plus read-tier platform visibility.

### Ownership model (where does a new feature belong?)

```
Applications  own  Business Logic
Platform      owns Coordination
Runtimes      own  Execution
Adapters      own  Communication
Plugins       own  Extensibility
```

### Future runtime ownership tracks

Every future runtime (Docker, Git, File, Kubernetes, Database) must be assigned one of:
core-team-built / community-contributed / customer-built, before it moves from "future" to
a scheduled implementation. None are assigned yet.

***

## Open Items (known unresolved — don't let a feature quietly re-decide these)

- **Tenant design** — multi-tenancy isolation semantics beyond the plugin-permission split
  above are not fully specified.
- **Plugin Marketplace operational details** — review SLA for Verified tier, revocation
  handling for already-installed malicious plugins, monetization, and runtime sandboxing are
  all explicitly out of scope in the current design.
- **Per-runtime ownership track assignment** — the framework exists; which specific track
  each future runtime lands on hasn't been decided.

***

## Decisions Log

Append here as each `feature-pipeline` run settles something. Format: `<date> — <topic> — <decision>`.

- 2026-08-03 — expected-origins backend-runtime enforcement (drift D-54) — backend-runtime now enforces origin binding on its sdk-browser path, closing the last gap of the T5 Q2 / W2 Q4 lock. `originMatches` (CID:origin-001) moved from gateway-core to a new shared package `@platform/origin` so both doors consume one primitive without a package cycle (gateway-core depends on backend-runtime; same precedent as `@platform/errors`). gateway-core re-exports it (`src/index.ts:9`) — public surface unchanged. Enforcement: upgrade `Origin` captured per connection; after `verifyToken` succeeds, browser Origin must match `claims.expectedOrigins` (RFC 6125 §6.4.3 match, deny-by-default when claim absent) → mismatch sends `sdk.auth.error {code:"ORIGIN_MISMATCH"}` then close 1008, no accepted event; Node clients without an Origin header bypass. Mirrors adapter-websocket's post-verify path exactly — both doors now share the primitive AND the semantics. Source: D-54 log; adapter-websocket `auth.ts:59-62`; sdk-browser T5 Q2; W2 sub-Q 4.
- 2026-08-03 — expected-origins mint side (drift D-50) — `IssueTokenRequest` gains optional `expectedOrigins: string[]`; `issueToken` + `auth.token.issue` mint the signed claim when present (empty → absent); CLI `token issue` gains repeatable `--origin <url>` and comma-list `--origins <csv>` (union, trim, drop-empty, dedupe first-occurrence order; no flags → claim omitted); no `--kind` flag (enforcement keys on Origin-header presence, W2 Q4); no mint-time URL/wildcard validation (invalid patterns never match → deny-by-default is the safety net). Enforcement follow-up (backlog row 24, drift D-54) CLOSED 2026-08-03 — see D-54 entry above. Source: grill-lite `docs/features/expected-origins/GRILL-expected-origins.txt`; T5 Q2; W2 Q4; D4.
- 2026-08-03 — dashboard-core wayfinder grill (D3, UI shape — autonomous lock, user-delegated decision authority) — D3 closed; the dashboard-core map is DONE. **Stack:** plain HTML/CSS/JS single page, no framework/build (repo convention). **Static server:** dashboard package's own HTTP server, `dashboardPort` default **7200** via `@platform/agentide` factory (mirror `DEFAULT_ADAPTER_MCP_PORT` 7100), bound 127.0.0.1; routes `GET /` (index.html with freshly minted token injected) + `GET /assets/*`; NO data API (Q2 — adapter-websocket is the only door). Guard: adapter must not take 7200. **Layout:** header (connection status pill, tenant, token hint) + 4 panels (Sessions, Plugins, Capabilities, Health), CSS grid. **Wire:** the page speaks the ADAPTER envelope (`auth`/`auth.ok`/`auth.error`, `invoke`/`invoke.result`/`invoke.error`, `subscribe`/`event` — W2 Q1/W4), NOT sdk-browser's `sdk.auth` wire (sdk-browser is the backend-runtime transport; the dashboard is an adapter client) — small vanilla-JS WS client in served assets. **Open sequence:** connect → auth → 4 invokes (mode:"call") → snapshots → subscribe `["session.*","plugin.*","capability.*"]` → live render. `system.*` NOT subscribed (no producers, D5); Health polls `gateway.status` every **30s**. **Lifecycle:** sdk-browser patterns as behavior precedent — backoff 1→30s ±20% jitter, pagehide deliberate disconnect, hidden→pause reconnect, 1008/auth.error terminal (no reconnect). **States:** per-panel empty text; gateway-down banner + stale markers; token-expired → "reload the page". **Errors:** `invoke.error` code+message verbatim per panel. Source: D3 ticket resolution; adapter W2 locks; `docs/wayfinder/dashboard-core/tickets/ui-shape.md`.
- 2026-08-03 — dashboard-core wayfinder grill (D4, token handling — autonomous lock, user-delegated decision authority) — D4 closed. **Mint:** per page load, in-process — dashboard package calls operator API `gateway.issueToken({tenantId, callerId:"dashboard-bot", scope:["platform.*.read"], expectedOrigins:["http://localhost:7200","http://127.0.0.1:7200"]})` at every `GET /`, token injected into the page; memory only, no localStorage; CLI mint stays fallback/CI. **Origin binding:** `expectedOrigins` claim (T5 Q2 permanent; adapter W2 Q4 enforces — browser Origin present → claim REQUIRED, exact match, mismatch → `auth.error "origin mismatch"` → 1008). Mint side is the MISSING half — `issueToken` mints no expectedOrigins and the CLI has no `--origin` flags → **drift D-50 (High)**; in-pack work: additive `IssueTokenRequest.expectedOrigins?: string[]` + CLI flags. **Scope:** `platform.*.read` sufficient (authz namespace wildcard). **Lifetime:** default 1h; refresh = reload = fresh mint; adapter re-`auth` (W2 Q3) NOT used in v1. **Localhost:** server binds 127.0.0.1; expectedOrigins carries BOTH localhost/127.0.0.1 forms. **Leak surface:** read-only ≤1h origin-bound memory-only — accepted residual risk, documented in PRD. Source: D4 ticket resolution; verified against code (`cli.ts:200-222`, `factory.ts:161-170`); `docs/wayfinder/dashboard-core/tickets/token-handling.md`.
- 2026-08-03 — dashboard-core wayfinder grill (D2, cap shape — autonomous lock, user-delegated decision authority) — D2 closed. **Locked `dashboard.view.*` contract** (REVISES Q2 shorthand `tier:"act"`+`permissions:["read"]` — unshippable: rank-null in authz, never satisfied by `platform.*.read`): namespace `dashboard.view.<view>` (4 caps: sessions/plugins/capabilities/health; settles D-44 naming), type `platform`, version `1.0.0`, owner `dashboard`, permissions `["platform.dashboard.read"]`, tier `read`. Thin passthrough wrappers re-invoking `Gateway.handleInvocation()` with an internal token (caller `dashboard`, scope `platform.*.read`, minted once at start via operator API — handleInvocation verifies a JWT per call); double audit intended (view trail `dashboard-bot` + backing trail `dashboard`). **NO session** — backing caps session-less (handle-invocation.ts:46-63), dashboard session impossible (session.create = write cap); the four view names join `SESSION_LESS_CAPABILITIES` (exact-name, additive). Errors: `GATEWAY_*` passthrough. Registration: package exports `DASHBOARD_CAPS` + `createDashboardHandlers(gateway)`; `@platform/agentide` factory wires when `config.dashboardPort` set; kernel seam = generic optional `extraOwnerHandlers` in `createGateway` config (kernel stays dashboard-agnostic). Source: D2 ticket resolution; verified against code (`authz.ts`, `caps.ts`, `dispatch.ts`, `handle-invocation.ts`); `docs/wayfinder/dashboard-core/tickets/view-cap-shape.md`.
- 2026-08-03 — dashboard-core wayfinder grill (D1, view scope + acceptance bar) — D1 closed. **Acceptance bar:** every in-v1 dashboard view needs a snapshot source AND a live-update story on the socket — missing either half defers the view. **v1 view list (4):** Active Sessions (`session.list` + `session.*` events — snapshot stub `[]` logged as drift D-45, must fix), Installed Plugins (`plugin.list` + `plugin.*`), Registered Capabilities (`capability.list/describe` + `capability.*`), Runtime Health (`system.*` + `gateway.status` — no events, periodic refresh, interval set by D3). **Deferred to future.md (4):** Metrics (D-46 placeholder zeros), Logs (D-47 no read cap), Errors (D-48 no listing cap), Browser Instances (D-49 zero backing). No in-v1 view needs aggregation beyond a thin wrapper. Source: user locks, verbatim; `docs/wayfinder/dashboard-core/tickets/view-scope.md`.
- 2026-08-03 — dashboard-core wayfinder grill (Q3/Q4, sequencing + scope) — Execution waits for adapter-websocket (Q2 lock removed the HTTP backdoor; nothing exists to consume before the adapter ships) — charting runs in parallel NOW, feeding adapter W3/W4/W6 decisions into dashboard D2/D3. Q4 REVISES Q1: the web dashboard UI moves INTO v1 — v1 = `dashboard.view.*` read caps (thin in-process wrappers calling `Gateway.handleInvocation()`, tier `act`, permissions `read`, owner `dashboard`) + web UI served by the dashboard package's own static server (HTTP GET page+assets only, no data API), all data over adapter-websocket (`invoke` + `subscribe`/`event`), browser-held read-only origin-bound token (read-only `dashboard-bot`, scope `platform.*.read`, `expectedOrigins` per sdk-browser T5 Q2). Ship date = adapter ship date. Supersedes the 2026-08-03 Q1 entry's UI deferral. Source: user locks, verbatim; `docs/wayfinder/dashboard-core/map.md`.
- 2026-08-03 — dashboard-core wayfinder grill (Q1, scope) — v1 ships ONLY the backend data plane: server-side component that polls platform-capabilities read-tier caps (`session.list`, `plugin.list`, `capability.list`, `system.health`, `gateway.metrics`) and aggregates the §14 views. The web dashboard UI is explicitly deferred to `docs/wayfinder/dashboard-core/future.md`. Data plane still needs an exit (next Q). Source: user lock, verbatim; `docs/Feature_Backlog.md` BI[13]; `docs/architecture/Agentide.md` §14.
- 2026-07-26 — project bootstrap — TypeScript/Node monorepo, npm workspaces, vitest for
  tests, flat-config ESLint. Reference stack for the whole platform core + first Backend SDK.
- 2026-07-26 — philosophy adoption — [PHILOSOPHY.md](/home/spanexx/Shared/Learn/Agent-Bridge-SDK/PHILOSOPHY.md)
  adopted as governing engineering philosophy. All future decisions measured against replaceability test.
- 2026-07-26 — event-bus (v1) — Wildcards: `*` = one segment, `**` = any depth.
  `event.handler_failed` carries original event + handler index + error. Mixed sync/async
  handlers are invoked in subscription order, and `event.*` remains bus-internal only.
- 2026-07-26 — event-bus (v2 upgrade) — Wildcards switched to prefix model: `*` as final
  segment matches any remaining depth; bare `*` matches everything. `**` removed. Event type
  renamed to `PlatformEvent` with added `id` (UUID) + `publishedAt`. Subscribe returns
  `Subscription` object with `.unsubscribe()` instead of bare function. Failure payload
  changed to `{ eventName, subscriberPattern, error: { message, stack? } }`. Error objects
  normalized before surfacing. Malformed wildcard patterns (e.g. `br*wser.*`) rejected at
  subscribe time. All 29 existing tests updated and passing.
- 2026-07-26 — session-manager grilling — State machine: Active ⇄ Suspended → Archived (soft-delete). Suspend/destroy: Gateway + Session Manager combined policy (A + C). Resume: session ID only (Gateway is sole caller). Timeouts: 5 min idle → Suspend, 30 min TTL → Archive, both configurable per-session. Resource tracking: direct registration via Session Manager, cleanup triggered via Event Bus (`session.cleanup_resources`). Events: `session.created`, `session.suspended`, `session.resumed`, `session.destroyed`, `session.cleanup_resources`. Cleanup ordering: `cleanup_resources` fires before `destroyed`.
- 2026-07-28 — platform-capabilities grilling (Q1, ownership) — Platform capabilities are registered under the owner of the *module* that owns them, not under a single "gateway" owner. session.\* → owner=`session-manager`, capability.\* → owner=`capability-registry`, tenant.\* / auth.\* / gateway.\* stay under owner=`gateway` (no separate module exists in v1). plugin.\* (when added) → owner=`plugin-manager`. Dispatch already routes these owners in-process (`dispatch.ts` lines 207-212); this decision just makes the registration match what the dispatcher expected.
- 2026-07-28 — platform-capabilities grilling (Q2, scope) — BI\[6] ships `plugin.*` (6 caps: install/list/uninstall/enable/disable/reload) and `system.*` (3 caps: info/version/health), plus the ownership migration from gateway-core's existing 16 caps. `runtime.*` (platform-owned runtime management) is punted until a Runtime Manager pack exists — no Runtime Manager in v1. `marketplace.*` is BI\[16]; `dashboard.*` is BI\[13]. Both stay separate backlog items.
- 2026-07-28 — platform-capabilities grilling (Q3, permission naming) — Every write-tier platform cap declares `platform.<domain>.write`. Every read-tier platform cap declares `platform.<domain>.read`. Renames: `platform.session.create/delete/write` → `platform.session.write`, `platform.tenant.write` stays, `platform.token.issue` → `platform.token.write`, plus new `platform.plugin.read|write` and `platform.system.read`. The wildcard scope `platform.*.read` covers every read-tier platform cap (authz tier-hierarchy already supports this).
- 2026-07-28 — platform-capabilities grilling (Q4, package location) — Ships as a new separate package `@platform/platform-capabilities` under `packages/platform-capabilities/`, not added to gateway-core. Same shape as `@platform/plugin-manager`: own package.json, own tests, own tsconfig, workspace ref in root tsconfig. Operators gain one extra import but read 25 caps in one place instead of mixing built-ins into the kernel package.
- 2026-07-28 — platform-capabilities grilling (Q5, registration wiring) — `@platform/platform-capabilities` exports `registerPlatformCapabilities(registry)` that registers ALL 25 platform caps: its own 9 new (`plugin.*` × 6 + `system.*` × 3) AND the migrated 16 from gateway-core (now under their real owners: `session-manager`, `capability-registry`, `gateway`). `createGateway()` calls `registerPlatformCapabilities(registry)` instead of the old `registerGatewayCapabilities`. The old function is removed from gateway-core — single source of truth for platform caps lives in the new package.
- 2026-07-28 — platform-capabilities grilling (Phase 0.5, uncertainty scan) — U1-U7 all classified. U2 (system.health depth) was the only detour candidate; user opted to skip the prototype and pick a default in IMPL: `system.health` returns `{status: "ok"}` in v1 single-process; "degraded" path deferred to v2 when there are runtimes to check. U3 (plugin.list tenant filter) and U6 (degraded test) punt to BI\[7] permission-tiering. U4 / U5 / U7 resolved inline (consistent with BI\[4] plugin-manager / standard read-tier exposure / semver + optional build-hash format).
- 2026-07-27 — plugin-manager grilling — Manifest shape: top-level key (`runtime:`, `service:`, or `developer:`) names the plugin type and contains the plugin's `id`. Only one of these keys per manifest. Install source: `plugin.install` accepts both a registry id (lookup against the future Plugin Marketplace) and a local source path (`--source <local-path-or-private-url>`). v1 implements the local-source path; registry-id path is designed for but stubs out with a clear "marketplace unavailable" error until the marketplace pack ships. Disable = soft pause (capabilities stay registered, new invocations rejected with "plugin disabled", in-flight finish). Update = swap install record, re-register capabilities; in-flight invocations complete against the old version, new invocations route to the new. v1 validation: manifest schema valid + capability name format valid + no collision with already-registered capabilities. Platform-version constraints, cross-plugin dependencies, and source integrity are deferred to later packs. Storage (v1): install records persist to `./data/installed-plugins.json` (id, version, source path, install timestamp, enabled/disabled). On startup Plugin Manager reads the file and re-installs each plugin; missing source file at startup = re-install fails for that plugin with a clear error (existing install record preserved, operator can fix the source and run `plugin.reload` to recover). `plugin.reload` is a separate command from `plugin.update` — same mechanics, different entry point: reload re-reads the install record's source and refreshes the version field if the manifest version differs, but leaves source path / id / install timestamp / enable-disable state alone. Uninstall fires `plugin.cleanup` first (plugin cleans up its own resources), then `plugin.uninstalled` (install record removed, capabilities unregistered). In-flight capability invocations complete against the old version; idle sessions lose access on next call. Events: `plugin.installed`, `plugin.updated`, `plugin.reloaded`, `plugin.uninstalled`, `plugin.enabled`, `plugin.disabled`, `plugin.cleanup` — all separate, all under the `plugin.*` namespace. Errors: structured `{ code, message, details }` shape, terminal-only (no `plugin.error` event in v1).
- 2026-07-27 — gateway-core grilling (Q1) — Adapter translation: each adapter (MCP/CLI/REST/WS) translates its native protocol into a single canonical `Capability Invocation` packet and back; the Gateway exposes one `handleInvocation(req) → response` function. All adapter-specific logic stays in the adapter; the Gateway's dispatch/auth/authz/session/routing logic lives in one place and can be exercised in tests by calling `handleInvocation` directly.
- 2026-07-27 — gateway-core grilling (Q2) — Auth strategy: bearer tokens (JWT) issued by the Gateway itself in v1. Operators invoke an `auth.token.issue` capability to mint a token; callers (SDKs, AI agents) present `Authorization: Bearer <token>` on every request. Tokens are signed by the Gateway (HS256 in v1), carry claims (`sub` = callerId, `scope` = permission strings, `exp` = expiry), and have a short lifetime (default 1h) with refresh. v1 is self-signed; upgrade to RS256 (public-key, multi-IdP) is a later concern.
- 2026-07-27 — gateway-core grilling (Q3) — Session lifecycle: explicit, caller-initiated. The Gateway exposes `session.create` / `session.destroy` as regular platform capabilities; the caller invokes `session.create` and receives a `sessionId`. All capability calls (other than session-management itself and read-only discovery) carry that `sessionId` in the canonical packet. Required-vs-optional split: `session.*` lifecycle calls and read-only discovery (`capability.*`, `plugin.list`, `plugin.describe`, `gateway.status`, `system.*`) do not require a session; `plugin.install/update/uninstall/disable/enable`, `business.*`, and `runtime.*` always require one. Matches MCP's connection-oriented agent model and lets runtime resources (browser tabs, temp files) be naturally session-scoped.
- 2026-07-27 — gateway-core grilling (Q4) — Authz algorithm: tier-hierarchy match, scoped per namespace. Runtime caps: `read` < `act` < `destructive` (higher tier covers lower). Platform caps: `read` < `write` (same). Business caps: exact match — each capability is its own action, no tier implied. Matching rule: for each permission the capability declares, look up the matching scope in the caller's token by `(prefix, tier)`; if the caller's tier rank ≥ the capability's required tier rank → allowed. If none of the capability's declared permissions are covered → deny with `INSUFFICIENT_SCOPE`. Lets ops tokens carry one high-tier scope covering many capabilities; capability authors declare the *minimum tier* required (one entry), not every tier that grants access.
- 2026-07-27 — gateway-core grilling (Q5) — Dispatch model: three paths keyed by owner prefix on the CapabilityRecord. (a) `platform-*` built-ins (`session-manager`, `plugin-manager`, `capability-registry`) → in-process direct call. (b) `plugin:<id>` runtime plugins → in-process via Plugin Manager, which returns the runtime's handler reference. (c) `backend-sdk-*` business capabilities → forward to a Backend Runtime component (in-process with the Gateway in v1) which maintains persistent connections to SDKs running in user apps. Gateway never holds SDK connections itself; it just calls `runtime.invoke(owner, capability, input)`. Backend Runtime encapsulates the SDK-transport complexity. Matches the architecture docs' flow (`Gateway → Backend Runtime → Backend SDK → Application` for business capabilities).
- 2026-07-27 — gateway-core grilling (Q6) — Audit logging: two complementary surfaces — durable + observable. (1) The Gateway appends one JSON record per capability invocation to `./data/audit.log` (configurable path), one JSON object per line, persistent. Record fields: `ts`, `caller.id`, `caller.scope`, `session.id` (if present), `capability.name`, `capability.version`, `owner`, `status` (`ok` / `denied` / `error`), `denyReason` or `errorCode`, `durationMs`. Input payloads are NOT logged (PII stays in the application). (2) Same record shape emitted on the Event Bus as `gateway.invocation` for downstream observability (dashboards, analytics plugins, future SIEM integrations). Denials and errors produce audit records too — Gateway never silently drops an invocation from the log. Event Bus failures don't lose audit records (file is durable copy); file write failures don't break the invocation (best-effort, error logged to stderr).
- 2026-07-27 — gateway-core grilling (Q7) — Rate limiting: per-caller token bucket, configurable rate and capacity. Each `callerId` (from token `sub`) gets its own bucket. Capacity default 100, refill rate default 10/sec, both configurable. Every invocation attempt — successful, denied, or errored — consumes one token. Empty bucket → `RATE_LIMIT_EXCEEDED` returned without dispatch. Buckets are in-memory (single-process in v1; restart resets). Buckets keyed on `callerId` (NOT sessionId) so a malicious caller can't open new sessions to bypass; NOT keyed per-capability for v1 (audit log already shows who spammed which capability; per-capacity buckets can layer on later if needed).
- 2026-07-27 — gateway-core grilling (Q8) — Tenant isolation: full tenant lifecycle in v1 (option B). Every token's `sub` is `{ tenantId, callerId }`; every audit record, rate-limit bucket, and session record carries `tenantId`. The Gateway refuses any cross-tenant operation. v1 ships `tenant.create`, `tenant.list`, `tenant.suspend`, `tenant.delete` capabilities + per-tenant admin tokens. Self-hosted operators ignore `tenant.create` and live with one auto-provisioned tenant (bootstrap token generated at install); hosted platforms use the full lifecycle to provision customer orgs. Reasons B over A in v1: hosted pack becomes purely additive (consumes gateway-core's tenant API); self-hosted users see extra capabilities they ignore (cosmetic); tenant enforcement is identical in both shapes — only provisioning differs. Token issuance API: `auth.token.issue --tenant <id> --caller <id> --scope ...`. Bucket key: `(tenantId, callerId)`. Cross-tenant session resume refused. Cross-tenant capability invocation refused.
- 2026-07-27 — gateway-core grilling (Q9) — Dispatch failure model: fail-fast, no silent retries, one stable `GATEWAY_*` error code per failure mode, every failure produces an audit record with `status: "error"`. Codes: `GATEWAY_CAPABILITY_NOT_FOUND` (typo / unregistered), `GATEWAY_PLUGIN_NOT_INSTALLED` (runtime plugin absent), `GATEWAY_PLUGIN_DISABLED` (runtime plugin paused), `GATEWAY_SDK_UNREACHABLE` (business SDK disconnected), `GATEWAY_MANAGER_UNAVAILABLE` (platform manager in degraded state), `GATEWAY_HANDLER_TIMEOUT` (handler exceeded timeout), `GATEWAY_INTERNAL_ERROR` (anything else). Each error response carries `retryable: boolean` so the caller can decide whether to retry; Gateway doesn't enforce a retry policy on the caller. Auto-reconnect / circuit-breaker on SDK loss lives in Backend Runtime, NOT in the Gateway — Gateway's job is to surface failures clearly; Backend Runtime's job is to keep connections healthy.
- 2026-07-27 — gateway-core grilling (Q10) — Capability version resolution: auto-latest by default, explicit pinning available. Canonical packet carries `capability: { name, version? }`. When `version` is omitted, the Gateway calls `capabilityRegistry.describe(name)` and gets the latest registered version. When supplied, `describe(name, version)` returns that specific version. Resolved version recorded in the audit log so operators see what actually executed even when callers pinned. Calling a non-existent version returns `GATEWAY_CAPABILITY_NOT_FOUND`. No deprecation concept in v1 (the Capability Registry doesn't model it); when marketplace ships with deprecation, the Gateway refines to "latest non-deprecated" without breaking default behavior.
- 2026-07-27 — gateway-core grilling (Q11) — Invocation semantics: strict sync only in v1. No submit-and-poll, no streaming, no in-flight invocation tracking beyond a timeout. Caller blocks on `handleInvocation(req)` and gets `{output}` or `{error}` back. Default handler timeout 30s (configurable per-call via `timeoutMs` field in the canonical packet). Long-running operations are modeled in the HANDLER, not the Gateway — e.g., a `migration.start` capability returns `{jobId}`; the caller then uses `migration.status` and `migration.result` as separate sync calls. Streaming, async submit-and-poll, and in-flight cancellation all deferred to v2+ (no v1 caller needs them; complexity is significant).
- 2026-07-27 — gateway-core grilling (Q12) — Distribution + default adapter: ship as a binary + Docker image + npm CLI, all built from the same source. Primary install is the one-liner `curl -fsSL https://agentide.io/install.sh | bash` which auto-detects environment (binary/Docker/npx), initializes `~/.agentide/data/`, generates a bootstrap operator token, starts the platform with MCP adapter listening on `localhost:7100`, and prints the token + connection URL. The MCP adapter is bundled in the default distribution but replaceable via `--no-default-adapters` or `--adapter /path/to/custom-adapter.js` — PHILOSOPHY.md "Nothing Is Special" applied. The adapter package (`@platform/adapter-mcp`) stays a separate npm package; the binary bundles it but doesn't own it. Day-2 operator lifecycle is CLI: `agentide status / logs / stop / start / upgrade` plus shells over gateway-core capabilities (`agentide tenant create / list / suspend / delete`, `agentide plugin install`, `agentide token issue`). No operator code, ever. Backlog implications: new pack `@platform/agentide` provides the composition + CLI + distribution. Tier 2 #5 gateway-core + #6 platform-capabilities + #7 permission-tiering stay as planned; `@platform/agentide` slots in between gateway-core and platform-capabilities as the operator entry point.
- 2026-07-27 — gateway-core grilling (Q6) — Audit logging: two complementary surfaces, same record shape. (a) Append-only structured file at `./data/audit.log` (configurable via `config.auditLogPath`) — one JSON object per line, includes `ts`, `caller.{id,scope}`, `session.{id}`, `capability.{name,version}`, `owner`, `status` (`ok`/`denied`/`error`), optional `denyReason` / `errorCode`, and `durationMs`. Best-effort write — file write failures do not block the invocation but are surfaced to stderr. (b) Event Bus event `gateway.invocation` with the same payload shape, so dashboards / analytics plugins / future SIEM integrations can subscribe without coupling to the file format. v1 logs input *shape* (capability name + version) but NOT the actual input payload — sensitive data stays in the application. Log rotation is external (logrotate) in v1; reserved `schemaVersion: 1` field for forward compatibility.
- 2026-07-28 — platform-capabilities ship (BI\[6]) — 25 caps registered under their real owners (12 gateway + 5 session-manager + 2 capability-registry + 6 plugin-manager). Permission split standardized to `platform.<domain>.<read|write>`. New wildcard `platform.*.read` covers every read-tier platform cap — implemented via a 3-line authz.ts fix in `tierCovers` (parts\[1] === '\*' → rank check). The 4-call `registerPlatformCapabilities` migration handles the upgrade path cleanly: phase 1 re-registers `gateway` with only its 12 caps (registry diff removes the 7 legacy caps under `gateway` whose owners moved); phase 2 registers the other 13 caps under their new owners. CLI gains `--owner` and `--tier` filters on `capability list`. `SESSION_LESS_CAPABILITIES` set extended to include `plugin.list`, `system.*`, `auth.token.*` per GRILL Q3. The legacy `registerGatewayCapabilities` is removed. Total: 307 tests pass (was 242). Zero new third-party deps.
- 2026-07-29 — permission-tiering ship (BI\[7]) — `read`/`act`/`destructive` tier hierarchy enforced end-to-end across 5 packages. `CapabilityTier` added to `@platform/capability-registry` types; `validateRecord()` rejects business caps with tier, runtime caps without tier, and wrong-tier values; `deriveTier()` helper for platform caps that don't declare one. `@platform/plugin-manager` ships `tierFromConvention()` verb lookup (READ/ACT/DESTRUCTIVE verb sets); `buildCapabilityRecords()` calls convention + accepts explicit override; unknown verb throws `PLUGIN_TIER_REQUIRED`. `@platform/platform-capabilities` declares explicit `tier:` on all 25 caps. `@platform/gateway-core` `authz.ts` adds tier-hierarchy `checkAuthz()` with namespace scoping (same kind + same namespace + caller rank ≥ required rank); `platform.*.<tier>` namespace wildcard already from BI\[6] covers every domain under one scope. `capability.list` filter now hides catalog entries the caller cannot invoke — a non-bootstrap caller cannot enumerate capabilities they can't call. `@platform/agentide` CLI `--tier` filter uses first-class `card.tier` field (fixed from permission-string parsing). Tier 2 complete. Drift review verdict "Minor Drift" — 7 gaps all resolved this session (stale IMPL status, "No new flags" claim, Scenario 4 error-vs-null, missing tier-convention unit tests, CLI filter parsing, sim `stageTier()` theater, missing `stageInvoke()`/`stageAudit()`); see `docs/drift.md` D-14 → D-27. Also resolved D-1 (session-manager docs reconciliation: `touch()` API contract, attach-permits-suspended, timeout minimum `>= 1`, touch-throws-on-non-active) as D-28. Total: 394 tests pass across 37 vitest files (was 383 before this session; +11 tier-convention tests). Zero new third-party deps. Unblocks Tier 4 `browser-runtime` and Tier 5 `dashboard-core` tier badges.
- 2026-07-29 — sdk-node ship (BI\[8]) — `@platform/sdk-node` shipped as the reference Backend SDK (Node/TypeScript). Connects to the Gateway over WebSocket via `WsClient` (with 30s reconnect backoff cap and ±20% jitter per PRD Scenario 5), registers Business Capabilities from a manifest, executes handlers locally on inbound invocations, and emits 8 lifecycle events on the Event Bus: `sdk.connected`, `sdk.disconnected`, `sdk.capability.registered`, `sdk.capability.unregistered`, `sdk.capability.rejected` (8th event added in Phase 7 for async Gateway rejection), `sdk.invoke.started`, `sdk.invoke.completed`, `sdk.invoke.failed`. Public surface: `createSdk(config)` → `SdkInstance` with `connect()`, `register()` (async — Gateway rejection surfaces via `sdk.capability.rejected`, not a synchronous throw), `invoke()`, `disconnect()` (sets `closed=true`; no auto-reconnect on explicit disconnect), `reset()`, `state()`. Wire types are concrete: `WirePrimitive | WireObject | readonly WirePrimitive[] | readonly WireObject[]` for handler input/output; meta on Logger is scalar-only `Record<string, string | number | boolean | null>`. Each SDK instance gets its own Event Bus so subscribers don't see events from unrelated SDKs. 55 tests pass across 7 vitest files. Drift review verdict "Minor Drift" — 5 gaps all resolved this session (8th event added to PRD-TRD table; `register()` contract rewritten as async event-driven; Scenario 5 rewritten to distinguish explicit `disconnect()` from unexpected Gateway drop; IMPL Phase 3 module-layout note pointing to `index.ts:121-130` for inlined `connect()` and `lifecycle.test.ts` for 9 consolidated tests); see `docs/drift.md` D-2 → D-9. Browser sim at `docs/features/sdk-node/simulate.html` drives the real SDK via `ws→globalThis.WebSocket` shim + dropper callback so reconnect is observable. Not yet wired to Gateway dispatch — `gateway-sdk-dispatch` (BI\[8b) replaces `GATEWAY_SDK_UNREACHABLE` with real `backend-sdk-*` owner routing. Adds one third-p...
- 2026-07-30 — gateway-sdk-dispatch ship (BI\[8b]) — `@platform/backend-runtime` shipped as the gateway-side counterpart to `@platform/sdk-node`. Closes the `backend-sdk-*` dispatch loop: Business SDKs register via WebSocket, the Backend Runtime accepts them with HS256 JWT handshake (same secret as gateway-core — duplicated `loadOrCreateSecret` to keep gateway-core internals private), accumulates caps per connection, and atomically re-registers the full list with the Capability Registry on each cap message so SDK's per-cap `register()` calls don't wipe prior caps. Two bus events: `sdk.connection.accepted { appId, gatewayUrl, latencyMs }` and `sdk.connection.closed { appId, reason: "explicit"|"dropped" }`. Wire protocol: `sdk.invoke { callId, name, input, sessionId? }` → `sdk.invoke.result { callId, payload }` or `sdk.invoke.error { callId, code, message }`. Error mapping: `HANDLER_NOT_FOUND` → `GATEWAY_CAPABILITY_NOT_FOUND`, `HANDLER_ERROR` → `GATEWAY_INTERNAL_ERROR`, 30s timeout → `GATEWAY_HANDLER_TIMEOUT { retryable: true }`, mid-invoke socket close → `GATEWAY_SDK_UNREACHABLE { retryable: true }`. Replace semantics: same `appId` reconnect closes the prior socket + drops its caps atomically; `stop()` does the same for every connected appId. `gateway-core` gains `backendRuntime?` ctx field + `createGateway(config)` opt-in (regression test confirms no-runtime config still returns `GATEWAY_SDK_UNREACHABLE`); `agentide` gains `backendRuntimePort?: number` on `CreatePlatformConfig` that auto-creates the runtime and wires `start()` after gateway boot + `stop()` before gateway teardown. End-to-end integration test (`agentide/src/__tests__/backend-runtime.test.ts`) exercises real `ws.WebSocket` client → auth → register cap → `gateway.handleInvocation` round-trip → `platform.stop()` server close. 41 vitest files / 439 tests pass; build/lint/typecheck/check-banned-types all clean. One new third-party dep: `ws@^8.18.0` (MIT, \~30M weekly downloads, same major as sdk-node). `sdk-browser` reuses the same wire protocol and event taxonomy — no new formats needed there. Phase 7 cleanup: 6 pre-existing lint warnings in `sdk-node`/`plugin-manager` resolved inline (unused imports + `events.test.ts` re-exported helpers).
- 2026-07-30 — gateway-plugin-dispatch ship (BI\[8a]) — Closes the `plugin:*` dispatch loop. `@platform/plugin-manager` gains `handleInvocation(name, input, sessionId)` + `handler-loader.ts` (Node ESM `import()` of `runtime.entry` at install time, stores `{ [capabilityName]: async (input, ctx) => result }` map, validates default export is an object of functions, falls back gracefully on import failure). Kernel swap: `gateway-core/src/dispatch.ts` no longer throws `MANAGER_UNAVAILABLE` for `plugin:*` owners — calls `pluginManager.handleInvocation()` and translates PM errors per Option B matrix: `PLUGIN_HANDLER_NOT_FOUND → GATEWAY_HANDLER_NOT_FOUND { retryable: false }`; `PLUGIN_HANDLER_ERROR → GATEWAY_HANDLER_ERROR { retryable: false }` with original error preserved in `details.originalError`; `PLUGIN_HANDLER_LOAD_FAILED → GATEWAY_HANDLER_NOT_FOUND`; anything else → `GATEWAY_INTERNAL_ERROR`. Two new public codes added to `gateway-core/src/errors.ts`. Three new PM events: `plugin.handler.loaded { id, version, loadedAt, ok, error? }`, `plugin.handler.error { id, capability, at, error }`, and `plugin.handler.unloaded { id }`. New `CreatePlatformConfig.cleanupTimeoutMs` (default 5000) lets tests/callers fast-fail the cleanup-confirm timeout when a plugin has no cleanup-confirm hook (the PM auto-proceeds if the plugin doesn't confirm in time). Phase 6 deliverables: drift sub-agent review (Minor Drift, 4 accepted items D-29 → D-33 logged in `docs/drift.md`) + post-impl sim at `packages/agentide/scripts/simulate-gateway-plugin-dispatch.mjs` (8/8 PASS in 0.6s) — pre-impl sim archived. 44 vitest files / 465 tests pass (+10 handler-loading +8 dispatch +8 e2e integration); build/lint/typecheck/check-banned-types all clean. Zero new third-party deps (Node ESM `import()` is stdlib). Unblocks `browser-runtime` (Tier 4) and any other runtime plugin; combined with BI\[8b], every capability declared in any tier is now reachable through `handleInvocation`.
- 2026-07-30 — sdk-browser T1 (wayfinder grill) — Capability surface = DOM-annotation model (dev annotates elements with `data-sdk-cap`); agent sees only registered caps, never DOM dump. UI state = live dev-controlled catalog scoped to current page (NOT a separate state object). Metadata location = inline `data-sdk-*` attributes on elements. Introspection = continuous MutationObserver (NOT one-shot scan). Invocation = CustomEvent DOM dispatch with form-fill fallback for `<input>`/`<textarea>`. Default tier = `act`, default version = `1.0.0`. The SDK is *registration + dispatch in-page*; the Browser Runtime is the *execution engine* — boundary documented in T7 (#14).
- 2026-07-30 — sdk-browser T2 Q1 (wayfinder grill) — No capability manifest file. `createSdk({ gateway, appId, token, observeRoot?, defaultTier?, defaultVersion? })`; cap inventory comes entirely from `MutationObserver` walking `data-sdk-cap` attributes. Closes ticket sub-questions 1-3 (manifest source, handler source, validation) as moot — no manifest to source, no handler map to load (handlers live as page `addEventListener` calls on the annotated element), no manifest schema to validate. Rules out for v1: caps without a DOM element (use sdk-node instead); programmatic cap registration without a DOM element (defer to v2). Source: T1 lock at `docs/CONTEXT.md`; sdk-node's `manifest.ts` shape at `packages/sdk-node/src/manifest.ts` is the parallel we're choosing not to mirror.
- 2026-07-30 — sdk-browser T2 Q2 (wayfinder grill) — SDK reads only `data-sdk-cap` from each annotated element; tier and version come from config-level defaults applied SDK-wide (`createSdk({ defaultTier: 'act', defaultVersion: '1.0.0' })`). No per-element overrides in v1 — same cap name registered with different tiers on different elements fragments the cap model into a debugging nightmare. Form-fill input mapping is per-element in shape but is a T4 (developer experience) concern, not a registration concern. Rules out for v1: `data-sdk-tier`, `data-sdk-version`, `data-sdk-input`, `data-sdk-target` per-element overrides. Source: T1 lock at `docs/CONTEXT.md:162` (authorizes the `data-sdk-*` *family* but did not enumerate members); T4 owns DX surface decisions.
- 2026-07-30 — sdk-browser T2 Q3 (wayfinder grill) — Initial scan of `observeRoot` on `createSdk()` (picks up pre-mounted annotated elements) + `MutationObserver` watching added/removed nodes and attribute changes (`subtree: true, childList: true, attributes: true, attributeFilter: ['data-sdk-cap']`). Default `observeRoot = document.body`; overridable via config. v1 does NOT pierce shadow DOM (devs attach their own observer per shadow root and forward via `sdk.observe(rootElement)`) and does NOT pierce iframes (cross-origin impossible; same-origin out of scope). Initial scan is required because most annotated elements live in static HTML, not lazy-loaded JS. Source: WHATWG DOM Living Standard (`MutationObserver` semantics); Q2 lock; sdk-node has no analog.
- 2026-07-30 — sdk-browser T2 Q4 (wayfinder grill) — Deduplicate cap registration: register one cap per `data-sdk-cap` name; SDK internally tracks the count of annotated elements per cap. Register on 0→1 transition (first element enters DOM), unregister on 1→0 transition (last element leaves). The Gateway doesn't care how many UI elements back a cap — only that the cap exists. "Which element receives the dispatched event when several are visible" is a T4 (DX) concern, not a registration concern. Rules out: N copies with disambiguating suffixes, `data-sdk-primary` hint, per-element cap identity. Source: `MutationObserver` add/remove symmetry; T1 lock; T4 owns DX.
- 2026-07-30 — sdk-browser T2 Q5 (wayfinder grill) — Fan-out dispatch: SDK dispatches a CustomEvent on every annotated element with that cap name; each listener receives `detail: { input, ctx }` and filters by input match (dev pattern: single delegated listener on `document` using `e.target.closest('[data-sdk-cap="..."]')`). The SDK has no way to know which element is "right" for a given input — only the dev knows via DOM context. Fan-out keeps the SDK dumb and the dev explicit. Rules out: SDK picking one element (first/last/visible all arbitrary), dev-supplied dispatch handler at registration time (mirrors sdk-node — choosing not to), per-element dispatch targeting via attributes (ruled out in Q2), `data-sdk-primary` marker (ruled out in Q4). Source: WHATWG DOM `CustomEvent` semantics; T1 lock; Q2 + Q4 locks.
- 2026-07-30 — sdk-browser T5 Q1 (wayfinder grill) — Auth transport = first-message body, same wire as sdk-node: `{ type: "sdk.auth", token }` sent immediately after `onopen`. Browser cannot set custom `Authorization` headers in `WebSocket` handshake (WHATWG spec); and the server's `backend-runtime` only trusts the first-message body anyway — sdk-node's `Authorization` header is silently dropped by `server.ts:108-111` (`isAuthMessage` only). Browser SDK skips the redundant header, sends the same `sdk.auth` body. Zero new server code. Rules out: `?token=...` query string (logs leak long-lived LLM-agent JWTs into server access logs, browser history, `Referer`); `Sec-WebSocket-Protocol` (subprotocol is for protocol multiplexing, surfaces in dev tools + proxy traces, needs a custom parser). Source: [client.ts:106-114](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/sdk-node/src/client.ts#L106-L114), [server.ts:108-111](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/backend-runtime/src/server.ts#L108-L111), [server.ts:293-295](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/backend-runtime/src/server.ts#L293-L295).
- 2026-07-30 — sdk-browser T5 Q2 (wayfinder grill) — **Permanent origin binding**: every browser SDK token MUST carry a signed `expectedOrigins: string | string[]` JWT claim. `backend-runtime` reads the claim after `verifyToken` succeeds; if inbound `Origin` header doesn't match (with `*.subdomain` wildcard support), the server closes the socket with code 1008 (policy violation). Mandatory, no opt-out. Solves the permanent threat of stolen-token-replay-on-rogue-origin (`acme.com`'s token used on `evil.com`). Dev mode handled by issuing tokens for `http://localhost:port` — the operator scopes the dev token to their actual dev origin, mirroring prod policy. Mobile webview `Origin: null` is a valid literal value (with warning at issuance). sdk-node tokens are exempt (no Origin in Node). `auth.token.issue` CLI gains `--kind browser` flag + `--origin` / `--origins` options; `--kind node` (default) skips the field. Rules out permanently: global Origin allowlist at `backend-runtime` (different problem, doesn't solve the threat), default-off origin binding (defense that's off when needed is no defense), CORS preflight emulation (WebSocket has no preflight), Origin as primary security boundary (JWT is primary; `expectedOrigins` is defense-in-depth claim). Source: T5 ticket sub-Q 2 re-framed; JWT RFC 7519 (claims are signed into payload); WHATWG Fetch (`Origin` header auto-attached by browser on cross-origin WebSocket handshake); existing `verifyToken` at [server.ts:111](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/backend-runtime/src/server.ts#L111) is the place to add the post-auth `Origin` check.
- 2026-07-30 — sdk-browser T5 Q3 (wayfinder grill) — Forward the caller's JWT **verbatim** to page handlers via the dispatched CustomEvent's `detail.ctx.token`, mirroring sdk-node's existing decision. Verified at [sdk-node/src/invoke.ts:101](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/sdk-node/src/invoke.ts#L101) (`ctx: { app: { id, name }; token: string; sessionId? }`) and [invoke.ts:147](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/sdk-node/src/invoke.ts#L147) (passes `ctx.token` straight into `makeCallContext` → handler receives raw token in `CallContext`). Browser SDK follows the same shape so a handler that runs in both contexts doesn't need two code paths. Rules out: (B) claims-only forwarding (breaks handler-side validation; future token-rotation depends on raw tokens being available to handlers); (C) both claims + raw token (duplicate surface — JWT parse is one line if claims are needed). The handler decides what to do with the token: validate via a backend API that has the gateway secret, pass through to a downstream service that re-validates, or ignore. Source: sdk-node ship note (BI[8] "forwards the caller's token to handlers verbatim"); T5 ticket sub-Q 3.
- 2026-07-30 — sdk-browser T5 Q4 (wayfinder grill) — **Browser-native WebSocket only**, no polyfill, no fallback. SDK uses `globalThis.WebSocket` directly. Throws on construction if missing (every supported browser since 2011 has it). The `ws` Node library is **not bundled** — its `package.json` pulls in `net`, `tls`, `buffer` (all Node-only); the sim-only `ws-shim.cjs` (drift D-13) is a test artifact for sdk-node's browser post-impl sim, not a production concern. Server-side `WebSocketLike` interface at [backend-runtime/src/types.ts:89-92](file:///home/spanexx/Shared/Learn/Agent-Bridge-SDK/agentide/packages/backend-runtime/src/types.ts#L89-L92) is just `send(data: string)` and `close()` — both methods native browser `WebSocket` already has, so no shim is needed at the type boundary either. Rules out: (B) `ws` polyfill fallback (Node-only dep, broken in browsers, fallback path never executes in any real environment); (C) fetch+long-poll fallback (reimplements the transport for an environment that doesn't exist); a `WebSocketLike` abstraction in the browser SDK (the browser has `WebSocket`, use it); a "no WebSocket" error beyond construction-time throw. Source: WHATWG WebSocket Living Standard (every browser since 2011); drift D-13 (`ws-shim.cjs` is sim-only); T5 ticket sub-Q 4.
- 2026-07-30 — sdk-browser T3 Q1 (wayfinder grill) — **Pause reconnect while tab is hidden; resume immediately on `visibilitychange` to "visible"**. Subscribe to `document.addEventListener('visibilitychange', ...)`. On `'hidden'`: clear any pending reconnect timer, mark state as "waiting for visible". On `'visible'`: if state was "waiting for visible" AND we want to be connected, fire the reconnect immediately (same backoff curve as sdk-node, just gated on visibility). The reconnect schedule (1s, 2s, 4s, 8s, 16s, 30s cap, ±20% jitter) is unchanged from sdk-node's pattern; visibility gates *when* it fires, not the curve itself. Rules out: (B) always-reconnect (throttled timers stack up in backgrounded tabs and all fire on visible-transition — wasted work, no benefit); (C) close-on-hide (too aggressive — drops cap registration on every tab switch, multi-tab same-origin loses caps, in-flight state churns); "smart" different-backoff-when-hidden mode (over-engineered — the user is the source of truth for "do I need this connection"). Source: WHATWG Page Visibility API (`document.visibilityState`, `visibilitychange` event); WHATWG HTML Living Standard background-tab throttling rules; sdk-node backoff schedule (BI[8]); T3 ticket sub-Q 1.
- 2026-07-30 — sdk-browser T3 Q2 (wayfinder grill) — **Treat socket as dead on `offline`; trigger reconnect immediately on `online`, skipping backoff.** `window.addEventListener('online', ...)` and `window.addEventListener('offline', ...)`. On `offline`: mark state as `'offline'`, clear any pending reconnect timer (saves a wasted attempt). On `online`: clear the `'offline'` state, **reset the backoff** (next reconnect uses the initial delay, not the next-in-curve delay — the previous backoff is stale; the failure was "old"), and if we want to be connected AND not currently connected, fire the reconnect immediately. The reset is the key new behavior: 30s of staring at a tab after a tunnel transit is bad UX. Rules out: (B) ignoring the events (let natural close + backoff handle it — wastes up to 30s after coming back online); (C) using events as hints without skipping backoff (backoff's purpose is to avoid hammering a failing server; on `online` the server isn't failing, the path was lost — skip is correct); polling `navigator.onLine` (events already cover it; polling is wasteful). Interacts cleanly with T3 Q1: if hidden AND offline, no fire; if visible AND online, fire immediately. Source: WHATWG HTML Living Standard (`window.online`/`window.offline` events, `navigator.onLine` getter); T3 ticket sub-Q 2 (stated default matches).
- 2026-07-30 — sdk-browser T3 Q3 (wayfinder grill) — **Best-effort `sdk.disconnect` on `pagehide`, bfcache-aware.** Listen for `pagehide`. If `event.persisted === true` (page is going into bfcache — LLM agent's "back" button or browser restoring a frozen page): do nothing, the WebSocket stays alive for fast restore. Else (real unload, LLM agent truly leaves or session ends): try to send `{ type: "sdk.disconnect", reason: "pagehide" }` via `WebSocket.send(...)`, then call `WebSocket.close(1000, "pagehide")` (1000 = normal closure). Don't await the send; the browser may close the socket before the message is flushed. Best-effort. The "user" here is the LLM agent's browser session (the web owner's page is where the SDK runs; the LLM agent drives the browser and triggers `pagehide` when it navigates away or ends the session). Rules out: (B) close-without-message (gateway has to wait for TCP timeout, agents calling the cap get `HANDLER_TIMEOUT` until cleanup); `navigator.sendBeacon` (C) (HTTP POST, wrong transport); using `unload` instead of `pagehide` (deprecated, bfcache-unfriendly); awaiting the send (browser won't wait, don't pretend it will); Service Worker persistence (out of scope for v1). Source: WHATWG HTML Living Standard (`pagehide` event, `PageTransitionEvent.persisted` for bfcache); WHATWG WebSocket (`close(code, reason)`, code 1000 normal closure); T3 ticket sub-Q 3.
- 2026-07-30 — sdk-browser T3 Q4 (wayfinder grill) — **Server-initiated ping only; browser SDK unchanged.** `backend-runtime` sends a protocol-level ping every 30s per socket. Browser auto-pongs at the protocol layer (the browser's `WebSocket` API handles ping/pong transparently — page JS never sees them). If `backend-runtime` doesn't see a pong within 10s, it closes the socket with code 1011 ("heartbeat timeout"). The browser fires a `close` event; the SDK's existing close handler triggers the reconnect. **Zero browser SDK code change.** Same mechanism works for sdk-node (Node `ws` library also handles ping/pong transparently). The browser-side `WebSocket` API is special: it cannot observe ping/pong at the JS layer, so "client-initiated" for the browser would have to mean app-level wire messages — and `setTimeout`-based heartbeats in backgrounded tabs are exactly the kind of thing that gets throttled, making them unreliable; the protocol-level ping is the right tool. Rules out: (B) client-initiated app-level heartbeat (duplicates what the protocol already does, more wire messages, less reliable in backgrounded tabs); (C) both (over-engineering); "no heartbeat" (silent dead-but-TCP-up connections are a real failure mode). Source: WHATWG WebSocket / RFC 6455 (protocol-level ping/pong, browser auto-pong); Node `ws` library (`socket.ping()` server-side, transparent pong); T3 ticket sub-Q 4.
- 2026-07-31 — application-entity ID generation (wayfinder grill, T1) — Application `id` is a ULID in the format `app_<26-char-ulid>`. The `app_` prefix is a value-type discriminator (lets grep / log filters / future entity types like `tenant_`, `session_` coexist without namespace collisions). ULID's first 10 chars = millisecond timestamp, so string sort == time sort — operator dashboards, audit log scans, and pagination all work without a secondary index. The `ulid` npm package (~5KB, no native deps) ships `monotonicFactory` for strictly-increasing ids within a single process. No coordinator needed for generation — multi-replica safe. Rules out: UUIDv4 (no sortability), UUIDv7 (sortable but more library complexity, no marginal benefit), numeric auto-increment (single-process only, blocks multi-replica pack), hash-derived (no operator predictability). Implementation seed: `packages/application/src/id.ts` — `createApplicationId(): string` returning `app_${ulid()}`. Source: `docs/features/application-entity/GRILL-application-entity.txt` (sortable-by-creation-time contract); `docs/wayfinder/application-entity/tickets/id-generation-strategy.md` (resolution). Mirrors the existing `createId()` pattern at `packages/session-manager/src/types.ts:39-45` (UUIDv4 shape there; ULID here — same function name, new package).

- 2026-08-01 — sdk-browser T6 Q2 (wayfinder grill) — package.json `exports` locked: `"type": "module"`, `"main"`/`"types"`/`"exports"` with `import` condition only, no `require` (browsers never require — a `require` condition would invite CJS interop bugs for zero users). The `browser` field (UMD/IIFE re-point) is HELD with T6 Q1 — it becomes real only if T4's prototype picks the `<script>` tag install path; one-line addition, no ESM rework. Mirrors sdk-node (`packages/sdk-node/package.json:6-14`). Source: T6 ticket sub-Q 2 (package-shape-and-dependencies).
- 2026-08-01 — sdk-browser T6 Q3 (wayfinder grill) — Dependencies: exactly one runtime dep, `@platform/event-bus` (verified browser-safe: pure TS, zero external imports, only `crypto.randomUUID()` + `Date.now()`); `@platform/backend-runtime` type-only via tsconfig `references` (`BackendValue` — same pattern as sdk-node, no runtime dep). Dropped: `ws` (T5 Q4 — native WebSocket), `yaml` (T2 Q1 — no manifest file to parse). devDeps mirror sdk-node plus **jsdom** (first DOM-touching package in the monorepo; no jsdom/happy-dom config exists anywhere today). No `@types/node`/`@types/ws` — zero Node APIs. tsconfig must add `"lib": ["ES2022", "DOM", "DOM.Iterable"]` — `tsconfig.base.json` has no `lib`. `check-banned-types.sh` applies unchanged. Source: T6 ticket sub-Q 3.
- 2026-08-01 — sdk-browser T6 Q4 (wayfinder grill) — Test runner: vitest + jsdom via per-file `/** @vitest-environment jsdom */` docblock — zero root config change (root `vitest.config.ts` is a single node-env config, stays untouched). Transport tests stub `WebSocket` via `vi.stubGlobal` (jsdom has no WebSocket implementation; the stub is a test fixture, not a polyfill — T5 Q4 stands). Rules out: per-package config (would force converting root config to vitest `projects`, touching all packages' test setup) and `@vitest/browser` for v1 (real browser binary in CI, slower, flakier). Source: T6 ticket sub-Q 4.
- 2026-08-01 — sdk-browser T6 Q5 (wayfinder grill) — No hard bundle-size cap for v1. Soft guideline: exactly one runtime dep + ESM-only output so consumer bundlers tree-shake. A hard cap would force new tooling (bundler + CI size gate) on a repo that ships plain `tsc` dist; the check is easy to add later if size becomes a problem. Source: T6 ticket sub-Q 5.
- 2026-08-01 — sdk-browser T4 D1 + T6 Q1 (wayfinder grill/prototype) — **Canonical v1 install = Shape A (bundler import):** `npm install @platform/sdk-browser` + `import { createSdk } from '@platform/sdk-browser'`; bundler resolves the bare specifier. The `<script>` tag / IIFE path (Shape B) is deferred to a v1.1 follow-up ticket with a named trigger (a real no-bundler consumer). **Resolves T6 Q1 (was HELD):** build output = ESM-only; no IIFE build, no `browser` field in v1. Decision grounded in the T4 prototype (`prototypes/sdk-browser-dev-experience/`, Playwright-verified 4/4): both shapes run identical dev code, so the choice is install-only; A matches the modern default, mirrors sdk-node's install model, one build (T6 Q2 ESM-only lock). Rules out: script-tag canonical (B), both from day one (C). Source: T4 ticket; prototype README; T6 Q1 entry.
- 2026-08-01 — sdk-browser T4 D2 (wayfinder grill) — **No framework-native helpers in v1.** No `useSdk()` (React), no Vue composable, no Svelte store — v1 is framework-agnostic (`createSdk()` + 8 lifecycle events + `onStateChange()`). Helpers are thin wrappers (~10 lines saved of a ~15-line setup) over a surface that already works in any framework; each is a new maintained surface with framework-specific traps (hooks lifecycle, SSR, stale state). Trigger to revisit: the first framework team that adopts sdk-browser and asks for the shortcut — then add that one framework's helper. Same deferral pattern as every other v2 item. Rules out: React-only `useSdk()` in v1; all-three helpers in v1. Source: T4 ticket; v2-deferral pattern across T1/T2/T3 grills.
- 2026-08-02 — sdk-browser T4 D3 (wayfinder grill, prototype) — **Connection-state surface = `sdk.onStateChange(cb)` + `sdk.state().connectionState`.** One dedicated callback, invoked only on real transitions, four states: `connecting | connected | reconnecting | disconnected` (no duplicate callbacks; sync getter for render-on-load). The 8 lifecycle events stay for tooling/logging; `onStateChange` is the UI-facing surface, not an event-bus subscription. v1 API adds exactly one function + one field on the existing `state()` getter — no framework helpers (D2), no new event. sdk-node parity: `state().phase` (`init|connected|registered|disconnected`) exists there but has no callback and no `reconnecting` state — its consumers are servers, sdk-browser's are UIs where "reconnecting" (hidden tab / offline, T3 gates) must be visible. Rules out: (B) fold into the 8 events only (forces every UI consumer to derive state across events — one missed event = wrong badge); (C) extra unified `sdk.state` event on the event-bus (internal surface, zero named consumers). Source: T4 ticket; prototype `sdk-core.js` (setConnState → stateCbs, both shape pages' badges, Playwright 4/4); sdk-node `src/types.ts` `Phase`; T3 grills.
- 2026-08-02 — sdk-browser T7 (wayfinder task) — **Frontend SDK / browser-runtime boundary locked in docs.** `@platform/sdk-browser` registers *application* capabilities from the page (DOM-annotation model); "UI state" defined (live dev-controlled catalog of the page's annotated caps, synced via MutationObserver — not a separate store); `browser.*` automation caps belong to `browser-runtime` (#12), never the SDK. `Agentide.md` §6 rewritten (responsibilities + examples `customer.read`/`order.submit` + "Boundary at a glance"), §7 Browser Runtime status flipped to "not yet built (backlog #12)", §3 "Applications expose" example de-blurred; `Runtime_Capabilities.md` Browser namespace flagged "(future — see browser-runtime #12)". No drift entry — patch corrects stale text to already-settled intent. **T7 was the last wayfinder ticket; map complete → `delivery: feature-pipeline` for sdk-browser.** Source: T7 ticket resolution; T1 lock (UI state); wayfinder map.md.
- 2026-08-02 — browser-runtime T6 (wayfinder grill) — **browser.wait semantics locked:** waits for a condition AND has a fixed-duration mode (both). Input = discriminated union on a `wait` field: `{ wait: 'selector', selector, state?, timeout? }` (state: `visible | attached | hidden`, default `visible`) OR `{ wait: 'time', ms }`. Condition mode composes Playwright `locator.waitFor({ state })` / `page.waitForLoadState()` per T1 research; duration mode is a sleep with no condition. Default timeout 30s, per-call `timeout?` override capped at 120s; duration mode has no timeout interplay. Timeout → error code `BROWSER_WAIT_TIMEOUT`, `retryable: true` (page may still load; agent may retry). Final error-code naming/registration lives in Capability contracts (T2). Source: T6 ticket; T1 research API mapping.
- 2026-08-02 — browser-runtime T2 (wayfinder grill) — **All 11 browser capability contracts locked (plain data, driver-first).** Selectors = CSS-only strings in v1 (`text=`/`role=`/`xpath=` layer in v2 behind the driver). Tab addressing = numeric `tabId`, optional on every per-tab cap (navigate/click/type/scroll/wait/screenshot/close), default = most recently active tab; first tab auto-created on launch with `tabId: 0`, ids increment per context. `browser.launch { mode?: 'headless'|'headed' }` default headless (headed routes to lazy headed pool). `browser.navigate { url, tabId?, newTab?, waitUntil?, timeout? }` → `{ tabId, url }` (newTab:true opens fresh tab). `browser.click { selector, tabId?, button? }` (left default). `browser.type { selector, text, tabId?, delayMs? }` — fill semantics default, delayMs opts into pressSequentially. `browser.scroll { direction, px?, tabId?, selector? }` — selector → scrollIntoViewIfNeeded, px → wheel, default down one viewport. `browser.close { tabId? }` — tab if given, else session context teardown; never kills the shared Chromium process. `browser.tab.open { url?, tabId? }` → `{ tabId }`; `tab.switch { tabId }` → `{ tabId }` (activate + bringToFront); `tab.close { tabId }` → `{ closed: true }`. Errors = `BROWSER_*` codes passed through the `GATEWAY_HANDLER_ERROR` envelope (gateway-plugin-dispatch), `GATEWAY_*` stays infra-only; retryable policy = timeout/race retryable (`BROWSER_WAIT_TIMEOUT`, `BROWSER_SELECTOR_NOT_FOUND/TIMEOUT`, `BROWSER_NAVIGATION_TIMEOUT`, `BROWSER_LAUNCH_FAILED`), misuse not (`BROWSER_NO_CONTEXT`, `BROWSER_ALREADY_LAUNCHED`, `BROWSER_TAB_NOT_FOUND`, `BROWSER_CLOSED`, `BROWSER_NAVIGATION_FAILED`). Screenshot input locked (`{ tabId?, fullPage? }`), output payload deferred to T3. Source: T2 ticket (10 grill Qs); T1 driver-first rule; T6 wait semantics; gateway-plugin-dispatch error mapping.
- 2026-08-02 — browser-runtime T3 (wayfinder grill) — **Screenshot payload locked: inline base64 first, session Resource over cap.** `browser.screenshot` returns discriminated `{ format: 'png'|'jpeg', mode: 'inline'|'resource', data?, resourceId?, bytes }` — `data` XOR `resourceId`, `mode` discriminates. Cap = 256 KiB raw image bytes (pre-base64): 256 KiB ≈ 85k LLM tokens worst case inline (base64 ~1.33× inflation, ~4 chars/token) — the cap is context protection; over it the image never enters the response, so never enters agent context. Input extends T2: `{ tabId?, fullPage?, format?: 'png'|'jpeg', quality?: number (default 80, ignored for png), mode?: 'inline'|'resource' }`, mode default `'auto'` (cap decides); caller can force resource to keep context lean. Forced inline + oversize → `BROWSER_SCREENSHOT_TOO_LARGE`, `retryable: false` (misuse, per T2 policy). Resources session-owned (session resource dir, `session.closed` cleanup via session-manager; no TTL, no delete cap). Audit logs shape only `{ mode, bytes, format }`, never image bytes, either mode. Screenshot = low-level observation primitive, not primary agent path. Delivery: decision-only. Source: T3 ticket (9 grill Qs, 3 rounds); T2 contract; context-window math.
- 2026-08-02 — browser-runtime T4 (wayfinder grill) — **Navigate is the agent's page-discovery sync point.** `browser.navigate` output extends T2's `{ tabId, url }` to `{ tabId, url, capabilities, capsSettled }`: browser-runtime waits for the page's registration to settle, so the agent gets the page picture in one round trip; settle detection = DOM-read at navigate (Playwright evaluate counting `[data-sdk-cap]` — shipped CAP_ATTR from sdk-browser observer.ts; stability re-read; **AUDIT F11 revision: the original "caps registered signal (event-bus, per session)" is reversed — the SDK's `sdk.capability.registered` fires on a page-local bus, invisible to the gateway process**). Settle timeout → return what's registered so far with `capsSettled: false` — never an error (plain pages without sdk-browser are legitimate, empty caps). Capabilities are **tab-scoped** via browser-runtime's per-tab cap snapshot (F9/Q5-REVISION: the shipped capability-registry is NOT modified; `capability.list({ tabId })` is served by the runtime). Re-read after in-page change: re-invoke `browser.navigate` (idempotent on same tab) or `capability.list({ tabId })`; `browser.page.read` cap ruled out. Known limitation (F12): two tabs of the same app evict each other at the gateway (backend-runtime single-slot per appId) — v1 non-goal, invisible to the agent via the per-tab snapshot. Agent loop (sdk-browser T1 steps 3–5) now bounded. Source: T4 ticket (5 grill Qs); AUDIT-vs-shipped-code.md findings 2/3/4; sdk-browser T1 loop.
- 2026-08-02 — browser-runtime T5 (wayfinder grill, resolved autonomously — user delegated, review later) — **Suspend keeps the BrowserContext + Chromium process alive.** Grounded in session-manager contract ("resources preserved across suspend/resume — browser tab still open, auth still valid"); resume = instant, zero state loss. Event subscription: one listener, three events — `session.suspended` no-op, `session.resumed` no-op, `session.destroyed` → close context + process (only teardown path — AUDIT F10: shipped event name; `session.closed` does not exist; resource purge via `session.cleanup_resources`). Cap calls during suspend: trust gateway resume-first (session-manager Flow 2 step 6 — Gateway resumes before routing); no `BROWSER_SUSPENDED` state, no queueing; archive mid-call → `BROWSER_CLOSED` retryable: false. Memory cost accepted (~150–300 MB idle Chromium); keep-alive knob = v2 candidate, not v1. Resume transparent to agent — no new cap, no flag. Source: T5 ticket; session-manager FLOW Flow 2 postconditions; chart-grill session model.
- 2026-08-02 — browser-runtime T7 (wayfinder prototype, resolved autonomously — user delegated, review later; prototype at `prototypes/browser-runtime-human-observability/`) — **v1 ships shape A "The window": human observability = headed mode + screenshot-on-demand, zero new surface.** Headed mode is the observation surface (T1 lazy pool: `browser.launch({ mode: 'headed' })` opens a real Chromium window; Xvfb on servers); screenshot-on-demand is the T3 cap, human-callable through the Gateway. No snapshot console (B) — "show me a picture of the session" is a dashboard sentence, dashboard-core (BI[13]) territory — and no event surface (C) — console/nav/pageerror feed is BI[13]'s logs scope; would need permission-tiering for page internals with no agent value (agent path = structured caps via sdk-browser, T4). Both human and agent use the same `browser.*` caps; human path adds a headed window, agent path adds sdk-browser coupling. Streaming/recording/remote viewing/event persistence/cross-session views → dashboard-core (BI[13]); Chrome DevTools (#14) stays out of scope. Source: T7 ticket; prototype verdict pending user review.
- 2026-08-02 — browser-runtime GRILL Q1 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **Binary strategy = `@playwright/browser-chromium` exact-pin direct dep.** Browser binary downloads at package install (postinstall) and is guaranteed present whenever `@platform/browser-runtime` is installed — install == usable, zero extra ops step. Rules out: `playwright-core install chromium` as a plugin-install step (easy to forget, silently breaks launch at runtime). Source: GRILL-browser-runtime.txt Q1; T1 research recommendation.
- 2026-08-02 — browser-runtime GRILL Q2 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **No hard concurrency cap in v1.** One Chromium process hosts many BrowserContexts (each session = one context); ~10–20 concurrent sessions per host documented as practical guidance (memory-bound); operators control the envelope via host env. No `BROWSER_LIMIT_REACHED` code. Consistent with T5 keep-alive-knob stance (no knobs until a real need); revisit in v2 if memory pressure becomes measurable. Source: GRILL-browser-runtime.txt Q2; map fog (concurrency limits).
- 2026-08-03 — websocket-adapter W1 sub-Q 1 (wayfinder grill) — **WS adapter is push-only in v1.** No `tools/call` pull over WS, no JSON-RPC envelope on the socket; MCP remains the pull door (duplicating it on WS buys nothing — 38 tested calls exist). Pull over WS is **demanded** for a future run, recorded in `docs/wayfinder/websocket-adapter/future.md` — triggers: WS-only deployment without MCP, single-socket pull+push clients; shape (JSON-RPC mirror vs WS-native envelope) is NOT locked and must be grilled in that run; never scope-trimmable. Accepted trade-off: v1 clients wanting pull+push open two sockets (MCP + WS). Source: W1 ticket sub-Q 1 lock; future.md; user wording "ok for now the websocket should be push only but create a future.md in the websocket adapter that demands for pull also in a future run". **REVISED 2026-08-03 (same session, re-open): pull is now v1, not future.** See next entry.
- 2026-08-03 — websocket-adapter W1 sub-Q 1 REOPEN (wayfinder grill, same session) — **WS adapter supports pull in v1 with a WS-native envelope.** Driver: `dashboard-core` BI[13] is a concrete v1 consumer that needs `capability.list` / `plugin.list` / `session.list` / `system.health` / `gateway.metrics` over a single socket — the "pull in future" defer was based on no demonstrated need, and the dashboard IS that need. Wire messages: `{type: "invoke", correlationId, name, input, sessionId?, mode: "call" | "stream"}` → `{type: "invoke.result", correlationId, output}` | `{type: "invoke.error", correlationId, code, message, details?}` | `{type: "invoke.partial", correlationId, output}` (only for `mode: "stream"`) → `{type: "invoke.end", correlationId}` (closes the stream). Concurrent invokes multiplexed by client-generated opaque `correlationId` (server echoes verbatim, replies may arrive in any order). Session: `sessionId` required per CONTEXT.md canonical packet (optional for read-only discovery). Streaming: `mode: "stream"` reuses W1 sub-Q 2 adapter-level streaming contract; `mode: "call"` returns one result frame. Scope: **universal — every v1 client can use `invoke`, no scoped opt-in.** MCP stays the canonical pull door for LLM agents (those clients want JSON-RPC); WS pull is for non-LLM clients that want one socket (dashboards, Node services, CLIs). W1 sub-Q 4 inherited unchanged: WS envelope ≠ JSON-RPC; `invoke` joins the existing `{type: ...}` discriminator family. Cross-ticket impact: W4 (wire schema) MUST lock the five `invoke*` envelope variants + correlationId contract; W6 (backpressure) covers per-connection outbound queueing for both `event` and `invoke.partial`; W5 (fan-out) now also routes per-call partial-progress topics. `future.md` rewritten — pull is no longer future; remaining future items: kernel-level streaming seam, `invoke.batch`, subprotocol versioning, MCP-shape compat. Source: W1 ticket reopen lock; dashboard-core GRILL Q1 (BI[13] tier-5 visibility); user wording "no i dont want scopped".
- 2026-08-03 — websocket-adapter W1 sub-Q 2 (wayfinder grill) — **WS adapter streams capability-handler progress in v1, adapter-level (Reading A).** Kernel `Gateway.handleInvocation()` stays single-shot per gateway-core Q11; the adapter translates a kernel single result into partial frames for `mode: "stream"` clients and one frame for `mode: "call"` clients. MCP stays single-shot (no contract change). `docs/wayfinder/websocket-adapter/future.md` records the kernel-level streaming seam as a future promotion path if a future run wants platform-native streaming. Source: W1 ticket sub-Q 2 lock; future.md; user wording "no!! this part we do in v1, not future" + "ok for now A".
- 2026-08-03 — websocket-adapter W1 sub-Q 3 (wayfinder grill) — **Subscription = top-level message on the socket.** `{type: "subscribe", topics: [...]}` / `{type: "unsubscribe", topics: [...]}`. Dynamic over the life of one socket (add/drop topics without reconnecting). Rules out: inferred from auth handshake (would block dynamic ops after open); pre-handshake REST call (extra round-trip, two protocols to coordinate). Wire schema (W4) needs a `type` discriminator — already implied. Source: W1 ticket sub-Q 3 lock; user wording "agreed top-level message".
- 2026-08-03 — websocket-adapter W1 sub-Q 4 (wayfinder grill) — **WS envelope deliberately differs from MCP.** MCP = JSON-RPC request/response. WS = `{type, ...}` envelope with a `type` discriminator (subscribe / unsubscribe / event / auth / auth.ok / error / pong / ...). Different clients, different doors; same platform, two products. Locks W4 wire schema: NOT JSON-RPC — flat `{type, ...}` shape keyed by `type`. Source: W1 ticket sub-Q 4 lock; user wording "agreed **different envelopes**".
- 2026-08-03 — websocket-adapter W1 closed. v1 surface: push-only adapter + adapter-level streaming (Reading A) + top-level subscribe/unsubscribe + WS-native envelope. Pull support demanded (`docs/wayfinder/websocket-adapter/future.md`). Next ticket: W2 (auth handshake). **REVISED 2026-08-03 (same session): v1 surface now includes pull via WS-native `invoke` envelope; future.md rewritten. See W1 sub-Q 1 REOPEN entry above.**
- 2026-08-03 — websocket-adapter W5 closed (research). Fan-out = per-(connection × topic) `bus.subscribe()`; outbound queue per connection REQUIRED (bus dispatch awaits `Promise.allSettled` at `packages/event-bus/src/index.ts:242-246` — awaiting `socket.send()` inside the bus handler would back-pressure the bus); unsubscribe is idempotent and safe mid-dispatch (snapshot at publish-start, AC-15). Re-use `validatePattern` from `@platform/event-bus` so client-side grammar matches server. Proposed layout: `packages/adapter-websocket/src/{server,registry,fanout,errors}.ts`. Full resolution: `docs/wayfinder/websocket-adapter/tickets/W5-resolution.md`.
- 2026-08-03 — websocket-adapter W2 sub-Q 1 (wayfinder grill) — **Canonical auth transport is JWT-in-first-message-after-onopen** (mirrors sdk-node's `sdk.auth` frame and sdk-browser T5 Q1 lock). Wire: `{type:"auth", token}` → `{type:"auth.ok", connectionId, claims:{sub:{tenantId,callerId},scope,expiresAt}}` | `{type:"auth.error", code, message}` then close 1008. JWT payload inherits the MCP contract: `sub: {tenantId, callerId}` (audit path), `scope` (permissions array, checked via `checkAuthz` at `packages/gateway-core/src/authz.ts:56`), `expectedOrigins` (REQUIRED for browser clients — same model as sdk-browser T5 Q2; server reads upgrade `Origin` before the auth frame, mismatch → 1008), `expiresAt` (standard exp). Rules out `Sec-WebSocket-Protocol` (token in upgrade headers = access logs / APM leak) and `?token=` query (browser history, `Referer` leak, CSRF vector). Origin binding is conditional on the upgrade carrying `Origin` (Node clients don't send it — no check). Cross-ticket impact: W4 must lock `auth` / `auth.ok` / `auth.error` envelope variants + `code` field (codes: `token expired`, `token invalid`, `token missing`, `origin mismatch`, `tenant suspended`); W3 topic authorization checks `scope` per `subscribe`; W6 backpressure doesn't apply to the auth frame (single small frame, processed first). Source: W2 ticket sub-Q 1 lock; sdk-node auth path in `packages/backend-runtime/src/server.ts:108-150`; sdk-browser T5 Q1 / Q2 locks. User wording "JWT-in-first-message-after-onopen (mirror the sdk-node / sdk-browser pattern)".
- 2026-08-03 — websocket-adapter W2 sub-Q 2 (wayfinder grill) — **Server holds the connection in a pre-auth state; only the `auth` frame is processed; all other frames are silently dropped until `auth.ok` is sent.** State machine per connection: `open → pre-auth → authenticated | auth-error-closed`. Non-`auth` frames in `pre-auth` → debug-logged + dropped (no socket close, no error reply — "drop, don't punish"). Hard 30s timeout on `pre-auth` → close 1008 (config knob, mirrors sdk-node's 30s backoff window). `auth.error` → send `auth.error` then 1008 close. W1 REOPEN ripple: `invoke` is in the post-auth accepted-set alongside `subscribe` / `event`; same pre-auth drop rule applies — clients MUST wait for `auth.ok` before sending any non-auth frame, or the frame is dropped silently. Token refresh: `auth` allowed at any time in `authenticated` state; claims replaced in place, no state machine reset, no re-handshake (forward to sub-Q 3 for the contract). Mirrors `backend-runtime`'s proven pattern (`packages/backend-runtime/src/server.ts:108-150`) — same `open → pre-auth → accepted | auth-error-closed` shape. Source: W2 ticket sub-Q 2 lock; user wording "agreed".
- 2026-08-03 — websocket-adapter W2 sub-Q 3 (wayfinder grill) — **Mid-connection token refresh is supported.** Same wire shape as initial auth (`{type:"auth", token}` → `{type:"auth.ok", connectionId, claims}` | `{type:"auth.error", code, message}` then 1008 close). On success, server atomically replaces per-connection claims (`tenantId`, `callerId`, `scope`, `expectedOrigins` re-checked against the *upgrade* `Origin` which is fixed for the connection lifetime — refresh cannot escape origin binding). Carries across refresh: connection, active subscriptions, in-flight `invoke` `sessionId`s, per-connection outbound queue, `connectionId`. Resets atomically: `scope` (subsequent `subscribe` / `invoke` use new permissions). Does NOT abort in-flight invokes. Refresh failure → close 1008 (atomic swap, no fallback to old token — once refresh fails, old claims are untrusted, because refresh is one motivation for revocation). Audit emits `connection.rotated` event per refresh (W4). Cross-cuts W3: existing subscriptions NOT torn down on narrowing refresh — operator's call to `unsubscribe` if desired. Rules out "reconnect-with-new-token" because reconnect loses active subscriptions, in-flight `invoke` calls, and back-pressure context. Mirrors sdk-node lifecycle test pattern. Source: W2 ticket sub-Q 3 lock; user wording "agreed with your recommendation".
- 2026-08-03 — websocket-adapter W2 sub-Q 4 (wayfinder grill) — **Origin allowlist enforced by default: per-token `expectedOrigins` JWT claim, exact match only.** Server captures the upgrade `Origin` at upgrade time (fixed for the connection lifetime — browser cannot change it post-upgrade); the comparison runs when the `auth` frame is processed (always the first frame, per sub-Q 2 — this resolves the sub-Q 1 shorthand "1008 before the auth frame is read", since the server can't know the claim until the JWT arrives). `Origin` present (browser client) → `expectedOrigins` REQUIRED; missing/empty claim on a browser token = deny-by-default (empty allowlist, every `Origin` mismatches); mismatch → `auth.error {code:"origin mismatch"}` then close 1008 (code already in W4's locked list). Exact string match only — no prefix/suffix/regex/wildcard (`https://dashboard.example.com` must NOT match `https://dashboard.example.com.evil.com`). `Origin` absent (Node client) → no check, `expectedOrigins` ignored; a Node client that sends `Origin` explicitly opts into browser-style binding (token must match, else 1008). Per-token allowlist at mint time, NOT server-global config (deployment-portable — the token is self-describing; a server-global list would couple the adapter to a deployment URL). `expectedOrigins` is an array — multi-origin tokens supported (dev + staging in one token). Mirrors sdk-browser T5 Q2 verbatim — this ticket is the server-side enforcement, T5 Q2 the client-side contract. Cross-ticket: W4 `auth.error` code `origin mismatch` confirmed as the reply path (no separate `origin.error` frame); W3 (subscribe authz is per-`scope`) and W6 (one header capture + one string compare) unaffected. Source: W2 ticket sub-Q 4 lock; sdk-browser T5 Q2 lock; user wording "agreed with your recommendation".
- 2026-08-03 — websocket-adapter W2 closed. All four sub-questions locked: (1) JWT-in-first-message-after-onopen (canonical auth transport), (2) pre-auth state machine + 30s hard timeout + silent drop of non-auth frames ("drop, don't punish"), (3) mid-connection token refresh with atomic claim swap (no fallback to old token on failure, `connection.rotated` audit event, in-flight invokes not aborted), (4) per-token Origin allowlist (exact match, deny-by-default for browsers, Node bypass). Auth handshake contract complete — the WS adapter's auth surface is fully locked. Next: W3 (subscription model), unblocked by W2 close.
- 2026-08-03 — websocket-adapter W3 sub-Q 2 (wayfinder, resolved autonomously — user delegated, review later) — **Topic vocabulary = VERBATIM event-bus topics.** Clients subscribe to the exact dot-namespaced topics the bus publishes (`session.*`, `capability.*`, `gateway.invocation`, `sdk.connection.*`, …). No curated second vocabulary — a rename table is a translation layer maintained forever (nothing-is-special, interfaces-forever, tiny kernel); `validatePattern` reuse (W5) only works with verbatim vocabulary; "curated is safer" is false safety (the gate is per-subscribe scope authz, not vocabulary secrecy). `event.*` reserved namespace rejected at subscribe time (`WS_INVALID_TOPIC`) AND filtered at fan-out as defense-in-depth. Source: W3 ticket sub-Q 2 lock; user delegation 2026-08-03.
- 2026-08-03 — websocket-adapter W3 sub-Q 3 (wayfinder, resolved autonomously — user delegated, review later) — **Prefix wildcards verbatim, event-bus grammar: `*` final-segment only, bare `*` matches everything, no embedded `*`/regex; client-side validation reuses `validatePattern`.** Authorization per-PATTERN at subscribe time (NOT per-event): required permission DERIVED as `platform.<firstSegment>.read` (bare `*` → `platform.*.read` — operator-grade), checked with the same `checkAuthz` semantics as capability invocations (W2 sub-Q 1 cross-ticket). Uniform derived rule = no mapping table = new event namespaces work without adapter changes; wildcards don't widen the surface (first segment is what's checked). Malformed pattern → `subscribe.error {code:"WS_INVALID_TOPIC", topics}`. Source: W3 ticket sub-Q 3 lock; event-bus `match.ts`; `authz.ts`; user delegation 2026-08-03.
- 2026-08-03 — websocket-adapter W3 sub-Q 4 (wayfinder, resolved autonomously — user delegated, review later) — **No session scoping in v1; tenant boundary = process boundary.** REVISION of the ticket's suspicion ("own session by default"): the auth frame has no sessionId claim (W2 sub-Q 1 claims = sub/scope/expectedOrigins/expiresAt), session payloads carry no tenantId and the session store isn't tenant-keyed (`create({ownerId, adapterType})` only) — tenant isolation is by process and the in-process bus only sees its own tenant's events. The dashboard (v1 consumer, `platform.*.read`) needs ALL sessions' events — a tenant-wide operator view matching `session.list` semantics. sdk-browser per-tab isolation is backend-runtime's job (D-43), not the adapter's. Clients needing their own session's events filter client-side (payloads carry sessionId where relevant). Source: W3 ticket sub-Q 4 lock; session-manager payloads; W2 sub-Q 1; user delegation 2026-08-03.
- 2026-08-03 — websocket-adapter W3 sub-Q 5 (wayfinder, resolved autonomously — user delegated, review later) — **Subscription lifecycle: `subscribe` idempotent (per-connection dedupe, `subscribe.ok` anyway — drop-don't-punish); `unsubscribe` of never-subscribed topic → `unsubscribe.ok`, NO error (REVISION of suspected default — bus unsubscribe already idempotent per W5); `socket.close` prunes all subs (snapshot-then-unsubscribe, W5) + clears outbound queue; refresh does NOT re-authorize or prune existing subs (point-in-time authz, W2 sub-Q 3 forward — operator sends explicit `unsubscribe` to revoke after narrowing); no per-connection subscription-count cap in v1.** W4 cross-ticket: adds `subscribe.ok`/`subscribe.error`/`unsubscribe.ok` frames; `subscribe.error` codes `WS_INVALID_TOPIC` (W5) + `WS_FORBIDDEN` (proposal, W4 finalizes). Source: W3 ticket sub-Q 5 lock; W5 resolution; user delegation 2026-08-03.
- 2026-08-03 — websocket-adapter W3 closed. Subscription contract complete: verbatim topics, prefix wildcards (`*` final segment only), derived per-pattern authorization (`platform.<first>.read` via checkAuthz), no session scoping, idempotent lifecycle, prune-on-close. W4 (wire message schema) unblocked. Next: W6 (backpressure) then W4.
- 2026-08-02 — browser-runtime GRILL Q3 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **Network isolation = Playwright defaults in v1.** Contexts already isolate cookies/storage/state (T1); per-context proxy/interception (`proxy: { server }`) is a v2 candidate. Rules out: launch proxy option in v1 (option surface + credential handling for zero current agent-loop value). Source: GRILL-browser-runtime.txt Q3; map fog (network/proxy isolation).
- 2026-08-02 — browser-runtime GRILL Q4 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **New error code `BROWSER_CRASHED`, `retryable: true`** (extends the T2 table by one code), surfaced through the same `GATEWAY_HANDLER_ERROR` envelope. Mid-invocation browser-process crash = definitionally a race, and T2's retryable policy is "timeout/race retryable, misuse not"; reusing `BROWSER_CLOSED` (retryable: false) would mislabel a recoverable condition as terminal and make agents abandon sessions a simple relaunch fixes. Precedent: `BROWSER_LAUNCH_FAILED` already retryable: true — a crash is the mid-flight sibling of a failed launch. Session marked dead on `disconnected`; next `browser.launch` auto-relaunches (T1). `BROWSER_CLOSED` keeps its meaning (intentional close via browser.close / session end), still retryable: false. Note: deviates from the agent's initial grill recommendation (reuse CLOSED), reversed during writing for policy consistency — flagged for user review. Source: GRILL-browser-runtime.txt Q4; T2 error policy; map fog (crash recovery).
- 2026-08-02 — browser-runtime GRILL Q5 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **`capability.list` tabId filter lands in THIS pipeline, same PR scope** (extends shipped capability-registry). `list()` gains an optional `tabId` filter, backward compatible (no-arg list unchanged). Rules out: deferring (ships a half-contract — T4's navigate output carries caps and the agent loop re-reads a tab's caps by tabId; forces a follow-up cross-pack change). Source: GRILL-browser-runtime.txt Q5; T4 lock. **REVISED 2026-08-02 (grill-with-docs audit vs shipped code): filter is served by browser-runtime from its per-tab cap snapshot; the shipped capability-registry is NOT modified** — see Q5-revision entry below.
- 2026-08-02 — browser-runtime GRILL Q5-REVISION (feature-pipeline audit vs shipped implementation, user's grill-with-docs mandate, resolved autonomously — user delegated, review later) — **`capability.list` tabId filter = runtime-served snapshot, registry UNTOUCHED.** The shipped registry keys records by name+version per owner (store.ts makeKey `name\x1Fversion`), CapabilityCard has no owner/tabId (types.ts), and register() replaces the owner's map (index.ts) — it physically cannot distinguish tab 0's caps from tab 1's; shipped sdk-browser has no tab awareness to pass as owner metadata. "Extend the shipped registry" as originally locked would need a key-dimension change + card schema change + sdk-browser tab awareness: a breaking change to a SHIPPED package for a need T4 already serves (navigate output carries per-tab caps; F7 same-url re-navigate = sanctioned re-read). Revision: browser-runtime keeps a per-tab cap snapshot (captured at navigate, T4) and serves `capability.list({ tabId })` from it; no-arg list passes through unchanged — backward compatible exactly as Q5 promised, zero cross-package surgery. Original Q5 answer superseded. Source: GRILL-browser-runtime.txt F9; capability-registry shipped code (store.ts, types.ts, index.ts); sdk-browser observer.ts; T4 ticket.
- 2026-08-02 — browser-runtime F8 instance disambiguation (feature-pipeline, resolved autonomously — user delegated, review later) — **v1 disambiguates multi-instance selectors with three pieces: `browser.query` read cap, `instance` param, `BROWSER_SELECTOR_AMBIGUOUS` error.** `browser.query { selector, tabId? }` → `{ matches: number, addresses: string[] }` — concrete CSS addresses (pid-anchored `[data-pid="202"] .add-cart` when the page has stable attributes, else nth-of-type chains), reusable verbatim in click/type; `matches: 0` is a RESULT, never an error (query exists to teach the agent, not to fail). `instance: i` (1-based) on `browser.click`/`browser.type` targets the nth match. Click/type with >1 matches and no `instance` → `BROWSER_SELECTOR_AMBIGUOUS`, retryable: false (misuse — caller must disambiguate, per T2 policy). 12th capability (11 from T2 + query, read tier). Same model as shipped sdk-browser's count-based dedup (observer.ts counts `data-sdk-cap` occurrences, one card per name — query is the runtime-side equivalent). Resolves the capability-contracts strict-mode open question at contract level. Source: GRILL-browser-runtime.txt F8; sdk-browser observer.ts; user choice of Option 3 ("disambiguate in v1").
- 2026-08-02 — browser-runtime GRILL Q6 (feature-pipeline grill, resolved autonomously — user delegated, review later) — **Sim surface = HTML** (interconnected-simulation skill): `simulate-pre.html` then a distinct `simulate.html` post-impl, same style as sdk-browser — terminal pane + state cards + live event log. Browser state is inherently visual (tabs, URLs, screenshots); matches sibling-pack convention. Rules out: CLI sim. Source: GRILL-browser-runtime.txt Q6; sdk-browser sim precedent.
- 2026-08-02 — browser-runtime Phase 0.5 sim findings (pre-impl simulation, verified Playwright 25/25 + demo) — **tabId uniqueness: launch seeds the tab counter at 1; ids increment per context and are NEVER reused** (sim bug found: launch forgot to seed → duplicate tabId 0; fixed + regression-tested; real code note: crash-relaunch = new context = fresh ids). **navigate on an occupied tabId replaces that tab's page — never an error, never a new tab; `newTab: true` opens a fresh tab** (T2 contract confirmed in sim). **browser.close { tabId } closes just that tab, context intact; omitted = session teardown** (T2 confirmed). **Async commands must be serialized** (sim uses a command chain; gateway already serializes per-invocation). **Explicit `resume` before a post-suspend cap call is redundant — gateway resume-first auto-resumes** (T5 confirmed in demo). Source: GRILL-browser-runtime.txt Phase 0.5 findings F1-F6; simulate-pre.html.
- 2026-08-02 — browser-runtime F7 destructive-navigation guard (Phase 0.5 pre-impl sim, user review of navigate semantics) — **`browser.navigate` on a tab with registered capabilities + a DIFFERENT url throws `BROWSER_NAVIGATION_DESTRUCTIVE` (retryable: false)** instead of silently replacing the page (pagehide → caps unregister destroys in-flight agent work, e.g. navigating www.google.com over www.fb.com mid-task). Same-url re-navigate stays allowed (T4 re-read sync point); `newTab: true` always allowed (fresh tab); plain tabs (no caps) navigate freely. Rationale: the runtime knows its own cap state and must not let an agent destroy live work by accident; misuse-class error per T2 policy. Extends T2 error table — 2nd new code (after BROWSER_CRASHED). Source: GRILL-browser-runtime.txt F7; simulate-pre.html; user review.
- 2026-08-02 — browser-runtime AUDIT F10 (grill-with-docs: sdk-browser vs shipped code, user's skepticism mandate) — **Error envelope: `GATEWAY_HANDLER_ERROR` carries the `BROWSER_*` code + retryable in details via an additive extension.** Audit found the shipped plugin path DROPS the handler error's code and retryable: plugin-manager wraps handler throws as `PLUGIN_HANDLER_ERROR` keeping only the message (`originalError: e.message`, index.ts:218-226); gateway-core maps to `GATEWAY_HANDLER_ERROR` with retryable defaulting false (dispatch.ts translatePluginError). The capability-contracts claim "preserves the BROWSER_* code in the structured details; caller-visible code = BROWSER_*" overreached its source doc (gateway-plugin-dispatch PRD-TRD only promises the message). Locked: ADDITIVE envelope extension (PRD-TRD scope) — plugin-manager preserves `originalErrorCode` + `retryable` when the handler throws an Error carrying them; translatePluginError passes them through; envelope code stays `GATEWAY_HANDLER_ERROR`; callers match on `details.browserCode` + `details.retryable`. Backward compatible (extra detail fields). Without it, Q4's retryable:true codes (BROWSER_CRASHED, BROWSER_WAIT_TIMEOUT...) are unexpressible. Cross-package (2 small edits in shipped packages) — flagged for user review. Source: AUDIT-vs-shipped-code.md finding 1; plugin-manager index.ts; gateway-core dispatch.ts.
- 2026-08-02 — browser-runtime AUDIT F11 (grill-with-docs, user's skepticism mandate) — **T4 caps-settled detection = DOM-read at navigate; "event-bus, per session" reversed.** The SDK's `sdk.capability.registered` fires on a page-local bus (createEventBus per createSdk) — invisible to the gateway process; no per-registration bus event exists gateway-side (backend-runtime only emits sdk.connection.accepted/closed, appId-keyed). Locked: browser-runtime reads the tab's DOM via Playwright `evaluate` counting `[data-sdk-cap]` elements (shipped CAP_ATTR, sdk-browser observer.ts); settle = stability re-read; timeout → `capsSettled: false`. Zero shipped changes; naturally per-tab; immune to the register-frame bug (see F11-b below). Source: AUDIT-vs-shipped-code.md finding 2; sdk-browser index.ts/events.ts; backend-runtime server.ts.
- 2026-08-02 — browser-runtime AUDIT F11-b (grill-with-docs, SHIPPED BUG FOUND — sdk-browser owner, drift D-40) — **sdk-browser's register wire frame is name-only and fails registry validation → "register-failed" close.** `sendRegister` sends `{ type, name }` only (sdk-browser index.ts:131); backend-runtime builds the record with version/description undefined; capability-registry validateRecord requires both → register() rejects → backend-runtime closes the socket (server.ts:190-196). sdk-node sends the full frame (index.ts:142-150) and passes. Net: a page using sdk-browser NEVER reaches the registry today; its caps never register. NOT fixed in browser-runtime scope (shipped package); fix = sdk-browser sends version+description+permissions (parity with sdk-node) or backend-runtime defaults. Reinforces Q5-revision (runtime snapshot doesn't depend on the registry) and F11 (DOM-read is the ground truth). Source: AUDIT-vs-shipped-code.md finding 3; drift D-40.
- 2026-08-02 — browser-runtime AUDIT F12 (grill-with-docs, user's skepticism mandate) — **Two tabs of the same app evict each other at the gateway — v1 non-goal.** backend-runtime's appId connection registry replaces the previous socket on duplicate appId (replacedSockets WeakSet; close → removeByOwner + capsByAppId.delete + rejectAllPending). T4's "two tabs distinguishable" holds only in browser-runtime's per-tab snapshot (Q5-revision). Keying gateway registrations by tabId = cross-package change, deferred; the snapshot makes it invisible to the agent loop in v1. **SUPERSEDED same day (drift D-43 resolved):** sdk-browser now sends a `tabId` in the `sdk.auth` frame (auto-generated per JS context unless `SdkOptions.tabId` is set); backend-runtime keys connections `appId:tabId`, so two tabs of the same app coexist at the gateway (same key still replaces). Source: AUDIT-vs-shipped-code.md finding 4; backend-runtime server.ts.
