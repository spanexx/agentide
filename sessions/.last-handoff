# Handoff — 2026-08-07: Release 0.4.0 SHIPPED + release skills updated

## What happened this session

1. **Release cut (milestone trigger).** `@spanexx/agentide@0.4.0`, `@spanexx/gateway-core@0.7.1`, `@spanexx/adapter-websocket@0.6.1` published to npm.
   - Release PR #57 (https://github.com/spanexx/agentide/pull/57) merged squash → `9711a0a`.
   - Per-package GitHub Releases: `agentide-v0.4.0`, `gateway-core-v0.7.0`, `adapter-websocket-v0.6.0`.
   - Bump provenance (path-based attribution): `e880560` feat(agentide) touched gateway-core factory.ts (D-78 mkdir) → gateway-core 0.7.0; `1a6bfca` touched adapter-websocket → 0.6.0. Scope did NOT gate bumps — see D-90.
   - Prepublish `+1` bumps: gateway-core 0.7.0→0.7.1, adapter-websocket 0.6.0→0.6.1 (agentide does not prepublish-bump).

2. **One failed dispatch, fixed.** First `release.yml` dispatch died 403 (D-69 trap): 9 of 15 packages were one patch behind npm (`errors` 0.4.0→0.4.1 collision). Fix committed `29765f4` (`fix(release): sync package.json + manifest to npm state` — repo versions set to npm versions so pnpm silent-skips them), re-dispatched → success.

3. **Skills updated** (both at `.agents/skills/` — outside any git repo, no commit needed):
   - `release-agentide/SKILL.md`: keyring/sandbox auth trap + `gh api user` preflight; NEW step 2b repo-vs-npm drift preflight (prevents the 403); step 3 CRITICAL corrected (attribution by changed paths, not scope); PR-update race note (bot updates ~30-60s after push); sync-back scope note; 3 new common-error rows.
   - `ci-cd-agentide/SKILL.md`: "Cut a release" step 2 corrected; new "Pre-flight before any release work" recipe; new trap row (sibling MINOR bumps).

4. **Drift D-90** logged (docs/drift.md, Open now 17): skill claim "scope gates bumps" vs reality "path-based attribution". Committed `45e9852` + pushed.

5. **Environment note:** gh token lives in OS keyring; sandbox blocks keyring socket. User disabled the sandbox — gh + git work unsandboxed now. If sandbox returns: `gh auth token > ~/.gh-token` then `gh auth login -h github.com --with-token < ~/.gh-token`.

## Repo state

- `agentide` on `main` @ `45e9852`, pushed, clean (sessions files dirty after this handoff — commit them).
- Outer `Agent-Bridge-SDK/` not a git repo (skills, prompt-log.md live there).
- No open `autorelease: pending` PRs. CI green. NPM_TOKEN present. Ruleset active, workflow perms `write`.

## Pending items (pick any)

1. **Backlog row 31** — from cli-quality-of-life handoff, still open (~5 min, `update-backlog` skill).
2. **D-85 doc fix** — PRD-TRD-cli-consumer-ux Scenario 5 step 6: "Exits 0" → "Exits 5" (one line, closes drift).
3. **D-86** — append IPv6 test case to `packages/agentide/test/url-default.test.ts` (`ws://[::1]:7300/ws`).
4. **D-88** — simulate.sh Scenario 2 section (batch `--session` workflow).
5. **consumer.ts split pack** — 407 lines > 350 cap (D-89, D-68 for cli.ts): extract `runWatch`/`runWatchInner` → `watch.ts`. Full feature-pipeline pack (GRILL → PRD-TRD → IMPL → sim → drift → handoff).
6. **dashboard-core placeholder app.js** — open drift item, fix in a future dashboard-core release (fileloc.ts resolution post-publish).

## Useful skills

`situation-awareness` (bootstrap), `update-backlog` (#1), `git-flow` (commits), `feature-pipeline` (#5), `release-agentide` + `ci-cd-agentide` (next release — preflight 2b now catches the 403 drift), `drift` (D-85/86/88 closeouts).
