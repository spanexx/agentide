# Handoff — 2026-08-07: adapter-core wayfinder map mid-flight (A1+A2 locked, A3 next)

## Where things stand

**Active effort:** wayfinder map for `@spanexx/adapter-core` — a shared package holding the
invocation pipeline every Adapter (door) stands on. Map: `docs/wayfinder/adapter-core/map.md`;
tickets under `docs/wayfinder/adapter-core/tickets/`. Local-markdown tracker convention
(dashboard-core precedent). All charting + research committed; main @ `1d1c8cf`, pushed,
clean except one user edit (below).

**Destination (locked):** adapter-core shipped; adapter-mcp + adapter-websocket server
pipelines migrate onto it with **zero behavior delta** (asserted today = frozen forever);
REST adapter (backlog row 10) as proof consumer → `delivery: feature-pipeline` at the end.

**Decisions locked (tickets — read before grilling):**
- A10 (research) `tickets/A10-research-streaming-patterns.md` — response channel w/ terminal:
  `single|stream|subscribe`, primitives emit/end/event, unary = stream of length one.
- A11 (research) `tickets/A11-research-duplication-inventory.md` — 16 duplicated files
  (2,222 lines), 14 test files, 2 sims.
- A1 `tickets/A1-shared-package-boundary.md` — "own bytes" rule (parse/render stay in door,
  everything between shared; MCP tool-card rendering stays in MCP; connection registry
  shared). adapter-core imports gateway-core at runtime; **doors import ONLY adapter-core**
  (re-exports). One-call setup `createAdapterPipeline({gateway, config, input, output,
  errors, response})`; transport lifecycle stays with the door. adapter-core emits NO events.
- A2 `tickets/A2-auth-pipeline.md` — `auth: { mode: "early" | "lazy" }` knob; early =
  verify once at open, identity cached for connection lifetime + pre-verify hook (origin
  binding) + `re-verify(token)` for refresh; lazy = kernel verifies per call.
  `decodeScopeFromToken` → core as `readClaims(token)` (shared with A6). Auth failure
  behavior FROZEN (close 1008s, auth.error text, ORIGIN_MISMATCH, MCP JSON-RPC errors,
  audit denied records).

Both grills appended to `docs/CONTEXT.md` Decisions Log (repo standing rule — do the same
after each ticket).

## Next session: claim A3 — Session resolution

Ticket: `tickets/A3-session-resolution.md` (open, unblocked — A1/A2 closed). Grilling
format as before (grill-with-docs, one question at a time, plain English, user answers
"i will go with your recommendation" — recommendation stands unless pushed back). Locked
context it builds on: sessions passed through by adapters today (WS frame sessionId, MCP
`_meta.dev.agentide/sessionId`); CLI consumer owns auto-mint (`agentide/src/session-mint.ts`,
D-79); kernel never mints implicitly (`session.create` cap). Decide: passthrough-only vs
auto-mint helper in adapter-core; "no sessionId" semantics; session lifecycle for watch.

Then frontier order: A4 (response strategy — A10 research ready), A5 (error envelope), A6
(capability lookup — includes readClaims), then A7 (WS migration) + A8 (MCP migration), A9
(REST proof). One ticket per session. After closing: update ticket (Status closed +
Resolution + delivery tag), map Decisions-so-far + frontier table, CONTEXT.md Decisions Log,
commit `docs(wayfinder): close A<N> …`.

## Standing facts (verified, no need to re-dig)

- Kernel contracts: `CanonicalInvocation` (`gateway-core/src/types.ts:56`, token REQUIRED,
  kernel verifies), `CanonicalResponse`, `GatewayErrorPayload`, `verifyToken`
  (`gateway-core/src/auth.ts:51`), `originMatches` (`@spanexx/origin`). Kernel is
  single-shot; streaming = adapter-side packaging (`adapter-websocket/src/invoke.ts:56`).
- CLI consumer imports `createWsClient, WsInvokeError, WsDoorMismatchError` from
  adapter-websocket (client half stays put). Backend-runtime `verify.ts` = deliberate local
  copy (out of scope).
- Publish pipeline: 16th package (release-please-config, release.yml filter,
  prepare-publish.sh, no-cjs pin).
- D-94 logged: glossary "only door" line vs MCP direct handleInvocation — reword CONTEXT.md
  Adapter row once A1's boundary ships.

## Watch-outs

- **`docs/architecture/Goals.md` has a USER edit in progress** (Primary Goal → "Build the
  runtime for autonomous organizations.") — uncommitted, DO NOT touch/revert; surface only.
- **Example app** (`example/`) has uncommitted changes: heavy console tracing
  (`platform.agent.ts` [cap]/[sdk] logs + service logs), .env refreshed (backup .env.bak),
  SDK refs swapped to registry (`^0.2.0`). User's call whether to keep/commit.
- Sandbox was disabled by the user this session — terminal runs unsandboxed (gh keyring
  works). If sandbox returns: `gh auth token > ~/.gh-token` then
  `gh auth login -h github.com --with-token < ~/.gh-token`.
- D-91 (narrow-token auto-mint gap) + D-93 (JWT exp in ms) open; example app logs a D-93
  note at boot.

## Skills for next session

`situation-awareness` (bootstrap), `grill-with-docs` + `wayfinder` (A3), `domain-modeling`
if terms drift, `drift` (before doc edits), `git-flow` for commits (or direct commit+push
as this session did — precommit hook runs typecheck/lint/build).
