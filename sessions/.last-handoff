# Handoff — 2026-08-07 close: adapter-core SHIPPED + released + validated; session wrap

## Effort status

**Wayfinder map `adapter-core`** — milestone reached. `docs/wayfinder/adapter-core/map.md`
(commit `bde889e`, pushed). Map is **dormant**, remaining frontier:

- **A8 — MCP migration** (`tickets/A8-mcp-migration.md`, open) — move adapter-mcp server
  pipeline onto adapter-core (cleanest migration; translate.ts already pure; A6 lookup +
  A5 error map + A4 seam ready; lazy auth becomes the first true lazy consumer — D-95).
- **A9 — REST proof adapter** (`tickets/A9-rest-proof-adapter.md`, open) — backlog row 10;
  `delivery: feature-pipeline` when claimed. Consumes the shipped `createAdapterPipeline`.

Closed + delivered: A1 boundary, A2 auth, A3 session, A4 response seam, A5 error envelope,
A6 capability lookup (all locked), A7 WS migration **DELIVERED & SHIPPED**, A10/A11 research.

## What shipped this session-cycle

- `@spanexx/adapter-core` **0.2.1** published (NEW — shared pipeline); `adapter-websocket`
  0.7.0→0.7.1 migrated; 8 internals +0.1 (engines/require feats). PR #58 merged, tags
  created, repo synced (`a64cc18`). Post-release validation: **22 pass / 0 fail / 2 warn**
  (report `/tmp/agentide-validation-0.4.0.md`).
- Review fixes: simulate.sh ANSI-strip; PRD-TRD/IMPL to shipped 3-key API; drift
  D-95..D-99 logged (`docs/drift.md`, Open now 26); release-please + workflow wired for
  16th package; **CI blocker fixed** — `adapter-websocket/tsconfig.json` was missing the
  `../adapter-core` project reference (`1639f67`; CI had been red since the migration).
- Glossary reword still pending for D-94 ("only door" line in `docs/CONTEXT.md`) — do it
  when A8 lands.

## Repo state

`agentide` main @ `bde889e`, pushed, clean. Example app (`example/`) uncommitted: console
tracing + .env refresh + registry SDK refs + one-line `Registered 11 caps` restore —
user's call to keep/commit. Skill files (`.agents/skills/`) outside git.

## Next-session entry points

1. Claim **A8 (MCP migration)** — grill per ticket format; then delivery (feature-pipeline
   pack on `docs/features/adapter-mcp/`).
2. Or **A9 (REST proof)** — spec → feature-pipeline (backlog row 10).
3. Or the **`Goals.md` primary-goal rewrite** the user has in flight (untouched with us).

Skills: `situation-awareness`, `grill-with-docs` + `wayfinder`, `drift`,
`post-release-validation` (validated ok), `release-agentide` (next release), `git-flow`.