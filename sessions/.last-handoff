# sdk-browser: T6 grilling LOCKED + T4 dev-experience prototype SHIPPED (Playwright-verified) — user decisions pending

**Date:** 2026-08-01
**Branch:** `main` (in `agentide/`)
**Status:** Wayfinder map 5/7 closed (T1/T2/T3/T5/T6). Frontier: **T4 (prototype done, decisions pending)** + **T7 (boundary doc)** → then `delivery: feature-pipeline` for `@platform/sdk-browser`.

## What this session did

1. **T6 (package shape) grilling — LOCKED** (record: `docs/features/sdk-browser/GRILL-sdk-browser.txt` §T6; mirrored to `docs/CONTEXT.md` Decisions Log 2026-08-01 entries):
   - Q1 **Build output: HELD** — pending T4 outcome; IIFE only if T4 picks `<script>` tag install
   - Q2 `exports`: `"type": "module"`, `import`-condition only, no `require`; `browser` field rides on Q1
   - Q3 Deps: ONE runtime dep (`@platform/event-bus` — verified browser-safe: pure TS, only `crypto.randomUUID`/`Date.now`), type-only `backend-runtime` ref for `BackendValue` (same pattern as sdk-node), jsdom dev-dep (first DOM package in monorepo), NO `@types/node`/`@types/ws`; tsconfig needs `"lib": ["ES2022","DOM","DOM.Iterable"]` (base has none)
   - Q4 Tests: vitest + jsdom via per-file `/** @vitest-environment jsdom */` docblock — zero root config change; WebSocket stubbed via `vi.stubGlobal`
   - Q5 Size: no hard cap; soft guideline (1 runtime dep, ESM-only)

2. **T4 (frontend DX) prototype — BUILT + VERIFIED** at `agentide/prototypes/sdk-browser-dev-experience/`:
   - `sdk-core.js` — simulated SDK core (DOM observation, connection-gated dedup registration, fan-out CustomEvent dispatch + `closest()` filter, form-fill fallback, 8 lifecycle events, `onStateChange()` connection badge)
   - `shape-a.html` (bundler import via import map) + `shape-b.html` (`<script>` tag + `PlatformSdk` global) — identical demo page (`demo-doc.js`) and identical core under both install shapes
   - `index.html` + `README.md` — comparison + open questions
   - **Interconnected sim**: `serve.mjs` (static + `/api/state` GET/PUT, serialized atomic writes) + `sim-state.json` (single source of truth) + `verify-state.mjs` CLI
   - **Playwright CLI suite**: `playwright.config.cjs` + `tests/t4.spec.cjs` — 4/4 PASS (index routes, shape A lifecycle, shape B lifecycle, cross-shape interconnection)
   - `file://` CORS banner added (module imports blocked on file URLs — must use `node serve.mjs`)

## OPEN — user decisions needed (T4 ticket "Resolution must record")

1. **Canonical install path**: Shape A (bundler import — recommended for v1), Shape B, or both? → decides T6 Q1 (HELD)
2. **Framework helpers** (`useSdk()` React etc.) in v1 or punt to v2? (recommended: punt)
3. **Connection-state surfacing**: keep the `onStateChange()` badge pattern from the prototype?

Once decided: record in `docs/wayfinder/sdk-browser/tickets/frontend-developer-experience.md` + `docs/wayfinder/sdk-browser/map.md`, then T7 (boundary doc, `sdk-browser-and-browser-runtime-boundary.md` ticket), then feature-pipeline.

## Git state

- HEAD `42dde14` (docs: application-entity grill + wayfinder map + CONTEXT.md ULID entry)
- **My changes (uncommitted — user's manual-commit preference):**
  - `M docs/CONTEXT.md` (T6 Decisions Log entries)
  - `M docs/features/sdk-browser/GRILL-sdk-browser.txt` (T6 section)
  - `M docs/wayfinder/sdk-browser/map.md` (T6 closed, 5/7, fog note updated)
  - `?? prototypes/sdk-browser-dev-experience/` (entire prototype, ~12 files)
- **Pre-existing dirty — DO NOT TOUCH (user rule §-1):** `M pnpm-lock.yaml`, `M tsconfig.json`, `?? packages/adapter-mcp/`, `M sessions/*` (this handoff log)
- 3 stashes on agentide — do NOT pop without permission

## Other in-flight thread (per previous handoff, Jul 31)

**T2 application-provisioning** (application-entity pack): plan approved, mode BUILD, Phase 1 not started. Source of truth: `docs/wayfinder/application-entity/tickets/application-provisioning.md` + `GRILL-application-entity.txt`. User may switch back to it at any point.

## Environment notes

- Playwright: `/tmp/sim-test/node_modules` (playwright + @playwright/test 1.55.1). Browser pinned: `playwright.config.cjs` → chromium-1228 `chrome-linux64/chrome` (runner's default 1193 headless shell NOT installed)
- Run prototype: `node agentide/prototypes/sdk-browser-dev-experience/serve.mjs` → http://127.0.0.1:7947/
- Test: `NODE_PATH=/tmp/sim-test/node_modules /tmp/sim-test/node_modules/.bin/playwright test --config=agentide/prototypes/sdk-browser-dev-experience/playwright.config.cjs`
- `sim-state.json` reset to clean after tests — verify with `node verify-state.mjs`
- Drift log: 0 open

## Suggested skills next

- `feature-pipeline` — after T4 decisions + T7, for the sdk-browser pack build
- `prototype` — only if user wants to iterate the prototype shape further
- `git-flow` — when user green-lights commits (docs commit + prototype commit)
- `situation-awareness` — session start re-orient
