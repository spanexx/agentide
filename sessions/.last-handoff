# Wayfinder map COMPLETE — sdk-browser way clear (T7 closed)

## What happened this session

Wayfinder work-through on the sdk-browser map. Claimed + resolved **T7 (sdk-browser and browser-runtime boundary doc, `wayfinder:task`)** — the last open ticket. Map is now **7/7 closed; way is clear**.

Patch applied (5 required changes + 1 same-boundary fix):
1. `docs/architecture/Agentide.md` §6 Frontend SDK — removed `browser.*` responsibilities/examples; now: register application caps (DOM-annotation), UI state (defined per T1: live dev-controlled catalog of page's annotated caps, MutationObserver-synced, NOT a separate store), browser communication (WebSocket, same wire as sdk-node), dispatch invocations as DOM CustomEvents. Examples = `customer.read`/`order.submit`.
2. §6 "Boundary at a glance" sentence added (SDK inside app registers app caps; browser.* via browser-runtime #12).
3. §7 Browser Runtime: "Status: built." → "**Status: not yet built (backlog #12).**" Backend Runtime stays built.
4. `Runtime_Capabilities.md` Browser Runtime namespace → "(future — see browser-runtime #12)" header; worked tier-table untouched.
5. Extra: §3 "Applications expose" example de-blurred (browser.navigate/click removed, pointer added).

No drift entry — patch corrects stale doc text to already-settled intent.

## Files changed (agentide/, uncommitted)

- `docs/architecture/Agentide.md` (§3, §6, §7)
- `docs/architecture/Runtime_Capabilities.md` (Browser Runtime header)
- `docs/wayfinder/sdk-browser/tickets/sdk-browser-and-browser-runtime-boundary.md` (closed + Resolution)
- `docs/wayfinder/sdk-browser/map.md` (7/7, frontier gone, Decisions-so-far T7, "Way is clear")
- `docs/CONTEXT.md` (Decisions Log T7 entry)
- `prompt-log.md`

## Next session — DELIVERY

**Way is clear. Route: `delivery: feature-pipeline` → GRILL for sdk-browser** (map destination: `@platform/sdk-browser` shipped v1 in `packages/sdk-browser/`). Tag recorded on T4 + T7 resolutions + map.

- Run `feature-pipeline` skill: grill-with-docs (GRILL) → PRD-TRD → IMPL → implement → post-impl sim → drift check → ship.
- GRILL inputs ready: 7 closed wayfinder tickets (`docs/wayfinder/sdk-browser/tickets/`), GRILL record `docs/features/sdk-browser/GRILL-sdk-browser.txt`, prototype `prototypes/sdk-browser-dev-experience/` (Shape A ESM, onStateChange, Playwright 4/4), sdk-node PRD-TRD as shape to mirror (`docs/features/sdk-node/PRD-TRD-sdk-node.md`).
- Key v1 locks: ESM-only, `createSdk({gateway,appId,token,observeRoot?,defaultTier?,defaultVersion?})`, DOM-annotation + MutationObserver, CustomEvent fan-out dispatch, `onStateChange` 4-state, globalThis.WebSocket only, expectedOrigins JWT claim (backend-runtime reads it), jsdom vitest per-file docblock, one runtime dep (`@platform/event-bus`), no framework helpers.
- Remaining fog (NOT ticketed, do not ticket): Service Worker persistence (likely out of scope v1), cap deprecation flow (cross sdk-node v4.2), window/tab boundedness (open question).

## Repo state (agentide/, HEAD 09d0025 = origin/main)

- Uncommitted M: `data/sim-state.json`, `docs/Feature_Backlog.html`, `docs/Feature_Backlog.md` (mcp-adapter backlog sync), `prompt-log.md`, `sessions/.handoff-log.md`, `sessions/.last-handoff`, + wayfinder batch (CONTEXT.md, Agentide.md, Runtime_Capabilities.md, 2 tickets, map).
- Nothing committed this session. User said "say the word → commit via git-flow" (prior session); still pending user go-ahead.
- 3 stashes — do NOT pop without user permission.
- `scripts/backlog/` (repo root, outside git) — mcp-adapter.js rewritten to shipped.

## Skills for next session

- `feature-pipeline` + `grill-with-docs` + `domain-modeling` (delivery)
- `git-flow` (commit backlog/wayfinder doc batch when user says save)
- `caveman` + `log-prompt` + `situation-awareness` (mandatory per AGENTS.md)
