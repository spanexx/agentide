{
  "name": "@spanexx/__tests__",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-language integration tests for Agentide \u2014 vitest suite that drives the real `platform` binary against the real `@platform/adapter-websocket` + `@platform/gateway-core` factory. Exists outside `packages/*/src/**` so it does not pollute any single package's source tree.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit --pretty"
  },
  "dependencies": {
    "@spanexx/adapter-websocket": "workspace:*",
    "@spanexx/event-bus": "workspace:*",
    "@spanexx/gateway-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "workspace:*",
    "@types/ws": "workspace:*",
    "typescript": "workspace:*",
    "vitest": "workspace:*",
    "eslint": "workspace:*",
    "@typescript-eslint/parser": "workspace:*",
    "@typescript-eslint/eslint-plugin": "workspace:*",
    "ws": "workspace:*"
  }
}
